╔════════════════════════════════════════════════════════════════╗
║                                                                ║
║   🔧 Dockerfile 수정 완료! 이제 빌드가 가능합니다! 🔧         ║
║                                                                ║
╚════════════════════════════════════════════════════════════════╝

┌────────────────────────────────────────────────────────────────┐
│ 🚀 서버에서 실행할 명령어                                      │
└────────────────────────────────────────────────────────────────┘

cd /root/uvis && \
git fetch origin genspark_ai_developer && \
git reset --hard origin/genspark_ai_developer && \
docker-compose build --no-cache frontend && \
docker-compose up -d --force-recreate frontend nginx && \
sleep 30 && \
echo "=== 배포 완료 확인 ===" && \
docker-compose ps && \
echo "" && \
echo "=== Backend Health ===" && \
curl -s http://localhost:8000/health && \
echo "" && \
echo "" && \
echo "=== Frontend Access ===" && \
curl -s -I http://localhost/ | head -5


┌────────────────────────────────────────────────────────────────┐
│ ✅ 수정 사항                                                    │
└────────────────────────────────────────────────────────────────┘

  Dockerfile 변경:
  ❌ Before: RUN npm ci --only=production
  ✅ After:  RUN npm install
  
  이유:
  - npm ci는 package-lock.json이 Docker 이미지에 필요
  - npm install은 package*.json 패턴으로 더 유연하게 동작
  - 빌드 에러 해결


┌────────────────────────────────────────────────────────────────┐
│ 📊 최신 Git 정보                                                │
└────────────────────────────────────────────────────────────────┘

  Commit:  8139d3c
  Branch:  genspark_ai_developer
  Message: fix(docker): change npm ci to npm install in Dockerfile
  PR:      #4


┌────────────────────────────────────────────────────────────────┐
│ ⏱️  예상 소요 시간                                              │
└────────────────────────────────────────────────────────────────┘

  전체: 약 4-5분
  
  - Git pull:           10초
  - Frontend 빌드:      3-4분 (npm install 포함)
  - 컨테이너 재시작:    30초
  - 안정화:             30초


┌────────────────────────────────────────────────────────────────┐
│ 🎯 성공 확인                                                    │
└────────────────────────────────────────────────────────────────┘

  ✅ Frontend 빌드 완료 (에러 없음)
  ✅ 컨테이너 모두 "Up" 상태
  ✅ Backend: {"status":"healthy",...}
  ✅ Frontend: HTTP/1.1 200 OK


┌────────────────────────────────────────────────────────────────┐
│ 📍 접속 URL                                                     │
└────────────────────────────────────────────────────────────────┘

  🌐 Frontend:     http://YOUR_SERVER_IP/
  📚 API Docs:     http://YOUR_SERVER_IP:8000/docs
  💚 Health Check: http://YOUR_SERVER_IP:8000/health


╔════════════════════════════════════════════════════════════════╗
║                                                                ║
║   🎊 위 명령어를 실행하세요! 이번엔 성공합니다! 🎊             ║
║                                                                ║
╚════════════════════════════════════════════════════════════════╝
