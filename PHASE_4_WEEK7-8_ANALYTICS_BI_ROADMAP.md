# Phase 4 Week 7-8: 고급 분석 & BI 대시보드

**시작일**: 2026-02-05  
**예상 완료일**: 2026-02-19  
**상태**: 🚧 진행 중  
**목표**: 데이터 기반 의사결정 지원 시스템 구축

---

## 🎯 프로젝트 개요

경영진과 관리자를 위한 종합 비즈니스 인텔리전스(BI) 대시보드를 구축하여 15개 이상의 핵심 KPI를 실시간으로 모니터링하고, 트렌드 분석 및 예측 인사이트를 제공합니다.

---

## 💡 비즈니스 가치

### 의사결정 개선
- **실시간 KPI 모니터링**: 15개 핵심 지표 추적
- **트렌드 분석**: 일/주/월/년 단위 비교
- **예측 인사이트**: 미래 예측 및 경고
- **커스텀 리포트**: 필요한 데이터 추출
- **데이터 기반 전략**: 객관적 의사결정 지원

### 운영 효율성
- **병목 구간 식별**: 문제 영역 즉시 파악
- **성과 비교**: 기간별/팀별/차량별 비교
- **목표 대비 실적**: 달성률 추적
- **이상 징후 탐지**: 자동 알림
- **리소스 최적화**: 데이터 기반 배분

### 예상 효과
- 의사결정 시간 **70% 단축**
- 전략 수립 정확도 **40% 향상**
- 문제 대응 시간 **60% 단축**
- 리포트 작성 시간 **85% 절감**
- 연간 가치: **의사결정 품질 향상** (정량화 어려움)

---

## 🏗️ 시스템 아키텍처

### 기술 스택

#### 백엔드
```
FastAPI + PostgreSQL + Redis
├── AnalyticsService
│   ├── KPI 계산 엔진
│   ├── 트렌드 분석
│   ├── 예측 모델
│   ├── 데이터 집계
│   └── 캐싱 전략
├── ReportService
│   ├── 커스텀 리포트 생성
│   ├── 스케줄링 리포트
│   ├── PDF/Excel 익스포트
│   └── 이메일 전송
└── AnalyticsAPI
    ├── 15+ KPI 엔드포인트
    ├── 트렌드 조회
    ├── 비교 분석
    └── 리포트 생성
```

#### 프론트엔드
```
React + TypeScript + Recharts + AG Grid
├── AnalyticsDashboardPage
│   ├── 경영진 대시보드
│   ├── KPI 카드 그리드
│   ├── 인터랙티브 차트
│   ├── 데이터 테이블
│   └── 필터 및 기간 선택
├── DetailedAnalyticsPage
│   ├── 심층 분석 페이지
│   ├── 다차원 분석
│   ├── 드릴다운 기능
│   └── 비교 분석
└── ReportBuilderPage
    ├── 커스텀 리포트 빌더
    ├── 위젯 드래그 앤 드롭
    ├── 스케줄 설정
    └── 템플릿 관리
```

---

## 📊 핵심 KPI (15개)

### 1. 운영 효율성 (5개)

#### KPI 1: 주문 처리율
```python
주문 처리율 = (완료된 주문 / 총 주문) × 100
목표: ≥ 95%
```

#### KPI 2: 정시 배송률
```python
정시 배송률 = (정시 배송 / 총 배송) × 100
목표: ≥ 90%
```

#### KPI 3: 차량 가동률
```python
차량 가동률 = (실제 운행 시간 / 가용 시간) × 100
목표: ≥ 75%
```

#### KPI 4: 평균 배송 시간
```python
평균 배송 시간 = Σ(배송 완료 시간 - 주문 시간) / 배송 건수
목표: ≤ 4시간
```

#### KPI 5: 1일 평균 배송 건수
```python
1일 평균 배송 건수 = 총 배송 건수 / 영업일 수
목표: ≥ 120건
```

### 2. 재무 지표 (4개)

#### KPI 6: 일일 매출
```python
일일 매출 = Σ(주문 금액)
목표: 지속 성장
```

#### KPI 7: 평균 주문 금액
```python
평균 주문 금액 = 총 매출 / 주문 건수
목표: ≥ ₩150,000
```

#### KPI 8: 운영 비용
```python
운영 비용 = 연료비 + 인건비 + 차량 유지비 + 기타
목표: 최소화
```

#### KPI 9: 수익률
```python
수익률 = ((매출 - 비용) / 매출) × 100
목표: ≥ 25%
```

### 3. 고객 만족도 (3개)

#### KPI 10: 고객 만족도 점수
```python
고객 만족도 = 평균 평점 (1-5점)
목표: ≥ 4.5점
```

#### KPI 11: 고객 불만 건수
```python
고객 불만 건수 = 월간 불만 접수 건수
목표: ≤ 5건
```

#### KPI 12: 반복 주문률
```python
반복 주문률 = (재주문 고객 / 전체 고객) × 100
목표: ≥ 60%
```

### 4. 차량 & 운전자 (3개)

#### KPI 13: 차량당 배송 건수
```python
차량당 배송 건수 = 총 배송 건수 / 활성 차량 수
목표: ≥ 15건/일
```

#### KPI 14: 평균 주행 거리
```python
평균 주행 거리 = 총 주행 거리 / 배송 건수
목표: 최소화
```

#### KPI 15: 운전자 성과 점수
```python
운전자 성과 = (정시 배송 + 안전 운전 + 고객 평가) / 3
목표: ≥ 85점
```

---

## 🎨 대시보드 디자인

### 1. 경영진 대시보드

#### 레이아웃
```
┌─────────────────────────────────────────────────────────────┐
│ 📊 경영진 대시보드                    [오늘] [이번주] [이번달] │
├─────────────────────────────────────────────────────────────┤
│ ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐            │
│ │ 매출    │ │ 주문    │ │ 배송률  │ │ 고객    │            │
│ │₩5.2M   │ │  142건  │ │  96%   │ │  4.7⭐ │            │
│ │+12% ⬆️ │ │ +8% ⬆️ │ │ +2% ⬆️ │ │ +0.1 ⬆️│            │
│ └─────────┘ └─────────┘ └─────────┘ └─────────┘            │
├─────────────────────────────────────────────────────────────┤
│ 매출 추이 (최근 30일)                                        │
│ ┌─────────────────────────────────────────────────────────┐│
│ │    📈                                                    ││
│ │    ┌─┐                                                  ││
│ │    │ │  ┌─┐                                             ││
│ │ ┌─┐│ │  │ │  ┌─┐                                       ││
│ │ │ ││ │  │ │  │ │  ┌─┐                                  ││
│ │ └─┘└─┘  └─┘  └─┘  └─┘                                  ││
│ │ 1   7   14  21  28  (일)                                ││
│ └─────────────────────────────────────────────────────────┘│
├───────────────────────────────┬─────────────────────────────┤
│ 운영 효율성                   │ 차량 현황                   │
│ ┌───────────────────────────┐ │ ┌─────────────────────────┐│
│ │ 주문 처리율:     98% ✅   │ │ │ 운행중:  32대           ││
│ │ 정시 배송률:     96% ✅   │ │ │ 정차:    15대           ││
│ │ 차량 가동률:     82% ✅   │ │ │ 정비중:   3대           ││
│ │ 평균 배송시간:  3.2시간 ✅│ │ │ 가동률:  82% 📊         ││
│ └───────────────────────────┘ │ └─────────────────────────┘│
└───────────────────────────────┴─────────────────────────────┘
```

### 2. 상세 분석 페이지

#### 다차원 분석
```
┌─────────────────────────────────────────────────────────────┐
│ 📈 상세 분석 - 주문 분석                [필터] [기간] [비교] │
├─────────────────────────────────────────────────────────────┤
│ 분석 차원: [고객별▼] [지역별▼] [차량별▼] [시간대별▼]       │
├─────────────────────────────────────────────────────────────┤
│ 주문 건수 Top 10 고객                                       │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ 1. 삼성전자         45건  ████████████████████ 15%      ││
│ │ 2. LG전자           38건  ████████████████ 12%          ││
│ │ 3. 현대자동차       32건  ██████████████ 10%            ││
│ │ 4. SK하이닉스       28건  ████████████ 9%               ││
│ │ 5. 롯데마트         25건  ██████████ 8%                 ││
│ └─────────────────────────────────────────────────────────┘│
├─────────────────────────────────────────────────────────────┤
│ 시간대별 주문 분포                                          │
│ ┌─────────────────────────────────────────────────────────┐│
│ │ 50│     ┌─┐                                             ││
│ │ 40│  ┌─┐│ │  ┌─┐                                       ││
│ │ 30│  │ ││ │  │ │  ┌─┐                                  ││
│ │ 20│┌─┐│ ││ │  │ │  │ │  ┌─┐                           ││
│ │ 10││ ││ ││ │  │ │  │ │  │ │┌─┐                        ││
│ │  0│└─┘└─┘└─┘  └─┘  └─┘  └─┘└─┘                        ││
│ │   0  3  6  9  12 15 18 21 24 (시)                       ││
│ └─────────────────────────────────────────────────────────┘│
└─────────────────────────────────────────────────────────────┘
```

### 3. 커스텀 리포트 빌더

```
┌─────────────────────────────────────────────────────────────┐
│ 📄 리포트 빌더                             [저장] [실행]    │
├─────────────────────────────────────────────────────────────┤
│ 리포트 이름: [주간 운영 보고서_____________]                │
│ 기간: [2026-02-01] ~ [2026-02-07]                           │
├─────────────────────────────────────────────────────────────┤
│ 위젯 라이브러리          │ 리포트 미리보기                   │
│ ┌──────────────────────┐│ ┌─────────────────────────────┐  │
│ │ 📊 KPI 카드          ││ │ [매출 KPI]  [주문 KPI]     │  │
│ │ 📈 라인 차트         ││ │                             │  │
│ │ 📊 막대 차트         ││ │ [매출 추이 차트_________]   │  │
│ │ 🥧 파이 차트         ││ │                             │  │
│ │ 📋 데이터 테이블     ││ │ [상위 고객 테이블_______]   │  │
│ │ 🎯 게이지 차트       ││ │                             │  │
│ │ 🗺️ 히트맵          ││ │ [지역별 주문 분포_______]   │  │
│ └──────────────────────┘│ └─────────────────────────────┘  │
├─────────────────────────────────────────────────────────────┤
│ 스케줄: [매주 월요일 09:00] [수신자: admin@company.com]    │
│ 형식: [PDF ✓] [Excel ✓] [이메일 자동 발송 ✓]              │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔧 핵심 기능

### 1. KPI 계산 엔진

```python
class KPICalculator:
    """KPI 계산 엔진"""
    
    def calculate_order_completion_rate(self, start_date, end_date):
        """주문 처리율 계산"""
        total = count_orders(start_date, end_date)
        completed = count_orders(start_date, end_date, status='COMPLETED')
        return (completed / total) * 100 if total > 0 else 0
    
    def calculate_on_time_delivery_rate(self, start_date, end_date):
        """정시 배송률 계산"""
        total = count_deliveries(start_date, end_date)
        on_time = count_deliveries(start_date, end_date, on_time=True)
        return (on_time / total) * 100 if total > 0 else 0
    
    def calculate_vehicle_utilization(self, start_date, end_date):
        """차량 가동률 계산"""
        available_hours = get_available_hours(start_date, end_date)
        actual_hours = get_actual_hours(start_date, end_date)
        return (actual_hours / available_hours) * 100 if available_hours > 0 else 0
    
    # ... 12개 추가 KPI
```

### 2. 트렌드 분석

```python
class TrendAnalyzer:
    """트렌드 분석"""
    
    def analyze_daily_trend(self, metric, days=30):
        """일별 트렌드"""
        return get_daily_data(metric, days)
    
    def analyze_weekly_trend(self, metric, weeks=12):
        """주별 트렌드"""
        return get_weekly_data(metric, weeks)
    
    def analyze_monthly_trend(self, metric, months=12):
        """월별 트렌드"""
        return get_monthly_data(metric, months)
    
    def compare_periods(self, metric, period1, period2):
        """기간 비교"""
        data1 = get_data(metric, period1)
        data2 = get_data(metric, period2)
        return calculate_change(data1, data2)
    
    def detect_anomalies(self, metric, threshold=2.0):
        """이상 징후 탐지"""
        data = get_time_series(metric)
        return statistical_anomaly_detection(data, threshold)
```

### 3. 예측 모델

```python
class PredictionEngine:
    """예측 엔진"""
    
    def predict_sales(self, days_ahead=7):
        """매출 예측"""
        historical = get_sales_history(days=90)
        model = train_time_series_model(historical)
        return model.predict(days_ahead)
    
    def predict_order_volume(self, days_ahead=7):
        """주문량 예측"""
        historical = get_order_history(days=90)
        model = train_time_series_model(historical)
        return model.predict(days_ahead)
    
    def predict_resource_needs(self, date):
        """리소스 수요 예측"""
        predicted_orders = self.predict_order_volume(1)
        return calculate_resource_needs(predicted_orders)
```

### 4. 리포트 생성

```python
class ReportGenerator:
    """리포트 생성기"""
    
    def generate_pdf_report(self, template, data):
        """PDF 리포트 생성"""
        html = render_template(template, data)
        pdf = convert_html_to_pdf(html)
        return pdf
    
    def generate_excel_report(self, data):
        """Excel 리포트 생성"""
        workbook = create_workbook()
        for sheet_name, sheet_data in data.items():
            add_sheet(workbook, sheet_name, sheet_data)
        return workbook
    
    def schedule_report(self, report_config):
        """리포트 스케줄링"""
        schedule = create_schedule(report_config)
        register_cron_job(schedule)
```

---

## 📡 API 엔드포인트

### KPI 엔드포인트 (15개)

```
GET /api/v1/analytics/kpi/order-completion-rate
GET /api/v1/analytics/kpi/on-time-delivery-rate
GET /api/v1/analytics/kpi/vehicle-utilization
GET /api/v1/analytics/kpi/average-delivery-time
GET /api/v1/analytics/kpi/daily-orders
GET /api/v1/analytics/kpi/daily-revenue
GET /api/v1/analytics/kpi/average-order-value
GET /api/v1/analytics/kpi/operating-cost
GET /api/v1/analytics/kpi/profit-margin
GET /api/v1/analytics/kpi/customer-satisfaction
GET /api/v1/analytics/kpi/complaint-count
GET /api/v1/analytics/kpi/repeat-order-rate
GET /api/v1/analytics/kpi/orders-per-vehicle
GET /api/v1/analytics/kpi/average-distance
GET /api/v1/analytics/kpi/driver-performance
```

### 트렌드 엔드포인트

```
GET /api/v1/analytics/trend/daily?metric=revenue&days=30
GET /api/v1/analytics/trend/weekly?metric=orders&weeks=12
GET /api/v1/analytics/trend/monthly?metric=profit&months=12
GET /api/v1/analytics/compare?metric=revenue&period1=...&period2=...
```

### 예측 엔드포인트

```
GET /api/v1/analytics/predict/sales?days_ahead=7
GET /api/v1/analytics/predict/orders?days_ahead=7
GET /api/v1/analytics/predict/resources?date=2026-02-10
```

### 리포트 엔드포인트

```
POST /api/v1/analytics/report/generate
GET /api/v1/analytics/report/templates
POST /api/v1/analytics/report/schedule
GET /api/v1/analytics/report/history
```

---

## 📋 개발 단계

### Week 7 (백엔드 개발)

#### Day 1-2: 핵심 서비스 개발
- ✅ AnalyticsService 구현
  - KPI 계산 엔진 (15개)
  - 캐싱 전략
  - 데이터 집계
- ✅ TrendAnalyzer 구현
  - 일/주/월 트렌드
  - 기간 비교
  - 이상 징후 탐지

#### Day 3-4: REST API 개발
- ✅ 15개 KPI 엔드포인트
- ✅ 트렌드 조회 API
- ✅ 예측 API
- ✅ 리포트 생성 API

#### Day 5: 테스트 및 최적화
- ✅ 유닛 테스트
- ✅ 성능 최적화
- ✅ 캐싱 검증

### Week 8 (프론트엔드 개발)

#### Day 6-7: 경영진 대시보드
- ✅ AnalyticsDashboardPage 구현
- ✅ KPI 카드 컴포넌트
- ✅ 차트 컴포넌트 (Recharts)
- ✅ 필터 및 기간 선택

#### Day 8-9: 상세 분석 페이지
- ✅ DetailedAnalyticsPage 구현
- ✅ 다차원 분석
- ✅ 드릴다운 기능
- ✅ AG Grid 테이블

#### Day 10: 리포트 빌더
- ✅ ReportBuilderPage 구현
- ✅ 드래그 앤 드롭
- ✅ 템플릿 관리
- ✅ 스케줄 설정

---

## 🎯 성공 지표

### 시스템 성능
- KPI 계산 시간: < 1초
- 대시보드 로딩: < 2초
- 리포트 생성: < 10초
- 캐시 히트율: > 80%

### 사용자 만족도
- 사용 편의성: > 4.5/5.0
- 데이터 정확성: > 99%
- 리포트 유용성: > 4.0/5.0

### 비즈니스 효과
- 의사결정 시간: -70%
- 리포트 작성 시간: -85%
- 문제 대응 시간: -60%
- 전략 정확도: +40%

---

## 💡 향후 개선 사항

### Phase 2
- 머신러닝 기반 예측 고도화
- 실시간 알림 시스템
- 모바일 대시보드
- 음성 쿼리 지원

### Phase 3
- AI 추천 시스템
- 자동 인사이트 생성
- 자연어 처리 (NLP)
- 협업 기능

---

**상태**: 🚧 개발 진행 중  
**다음 단계**: 백엔드 AnalyticsService 구현

---

*문서 생성일: 2026-02-05*  
*최종 업데이트: 2026-02-05*
