# 기사용 모바일 앱 개발 가이드

**작성일**: 2026-01-27  
**작성자**: GenSpark AI Developer  
**상태**: 🚧 개발 중 (기본 구조 완성)

## 📋 개요

Cold Chain Dispatch System의 기사용 모바일 앱 개발이 시작되었습니다. React Native 기반으로 Android 및 iOS를 동시 지원합니다.

## 🎯 주요 기능

### ✅ 구현 완료
1. **프로젝트 구조 설계**
   - TypeScript 기반
   - 컴포넌트 기반 아키텍처
   - 서비스/유틸리티 분리

2. **인증 시스템**
   - JWT 기반 로그인
   - AsyncStorage를 통한 토큰 관리
   - AuthContext Provider
   - 자동 로그인 체크

3. **API 서비스**
   - Axios 기반 HTTP 클라이언트
   - Request/Response 인터셉터
   - 자동 토큰 주입
   - 에러 핸들링

4. **화면 구현**
   - 로그인 화면
   - 홈 화면 (오늘의 배차)
   - 배차 상세 화면
   - 경로 카드 컴포넌트

### 🔄 진행 예정
5. **경로 상세 화면**
   - 도착/출발 체크
   - 작업 시간 기록
   - 메모 추가

6. **카메라 기능**
   - 상/하차 사진 촬영
   - 이미지 미리보기
   - 서버 업로드

7. **GPS 위치 추적**
   - 백그라운드 위치 전송
   - 실시간 위치 업데이트
   - 배터리 최적화

8. **푸시 알림**
   - Firebase Cloud Messaging
   - 새 배차 알림
   - 상태 변경 알림

9. **오프라인 지원**
   - 로컬 데이터 캐싱
   - 동기화 큐
   - 재연결 처리

## 🏗️ 프로젝트 구조

```
mobile-app/
├── src/
│   ├── components/        # 재사용 컴포넌트
│   │   ├── auth/
│   │   ├── dispatch/
│   │   └── common/
│   ├── screens/          # 화면 컴포넌트
│   │   ├── auth/
│   │   │   └── LoginScreen.tsx
│   │   ├── dispatch/
│   │   │   ├── HomeScreen.tsx
│   │   │   ├── DispatchDetailScreen.tsx
│   │   │   └── RouteDetailScreen.tsx
│   │   └── common/
│   ├── services/         # API 및 서비스
│   │   └── api.ts
│   ├── navigation/       # 네비게이션 설정
│   ├── hooks/           # Custom Hooks
│   │   └── useAuth.tsx
│   ├── utils/           # 유틸리티 함수
│   ├── types/           # TypeScript 타입
│   │   └── index.ts
│   └── assets/          # 이미지/아이콘
├── android/             # Android 네이티브
├── ios/                # iOS 네이티브
├── package.json
└── tsconfig.json
```

## 📦 기술 스택

### Core
- **React Native**: 0.73.0
- **TypeScript**: 5.0.4
- **React**: 18.2.0

### Navigation
- **@react-navigation/native**: 6.1.9
- **@react-navigation/stack**: 6.3.20
- **@react-navigation/bottom-tabs**: 6.5.11

### State Management & Data
- **Axios**: 1.6.0 (HTTP 클라이언트)
- **AsyncStorage**: 1.21.0 (로컬 저장소)
- **date-fns**: 3.0.0 (날짜 처리)

### 네이티브 기능
- **react-native-maps**: 1.10.0 (지도)
- **react-native-geolocation-service**: 5.3.1 (GPS)
- **react-native-image-picker**: 7.1.0 (카메라)
- **react-native-permissions**: 4.0.3 (권한)

### 푸시 알림
- **@react-native-firebase/app**: 19.0.1
- **@react-native-firebase/messaging**: 19.0.1
- **react-native-push-notification**: 8.1.1

## 🚀 시작하기

### Prerequisites

```bash
# Node.js 18+
node --version

# React Native CLI
npm install -g react-native-cli

# Android Studio (Android)
# Xcode (iOS, macOS only)
```

### 설치

```bash
# 프로젝트 디렉터리로 이동
cd mobile-app

# 의존성 설치
npm install

# iOS Pod 설치 (iOS only)
cd ios && pod install && cd ..
```

### 실행

```bash
# Metro 번들러 시작
npm start

# Android 실행
npm run android

# iOS 실행 (macOS only)
npm run ios
```

## 📱 화면 설명

### 1. 로그인 화면 (`LoginScreen.tsx`)

**기능**:
- 사용자명/비밀번호 입력
- JWT 토큰 발급 및 저장
- 자동 로그인 지원
- 에러 처리

**API**:
- `POST /api/v1/auth/login`

### 2. 홈 화면 (`HomeScreen.tsx`)

**기능**:
- 오늘의 배차 목록 조회
- 배차 상태별 색상 표시
- Pull-to-refresh
- 배차 상세로 이동

**API**:
- `GET /api/v1/dispatches/my?date=2026-01-27`

**화면 구성**:
```
┌─────────────────────────┐
│ 오늘의 배차             │
│ 2026년 01월 27일        │
├─────────────────────────┤
│ ┌─────────────────────┐ │
│ │ DISP-20260127-001   │ │
│ │ [진행중]            │ │
│ │                     │ │
│ │ 차량: V001          │ │
│ │ 주문: 3건           │ │
│ │ 팔레트: 15개        │ │
│ │ 중량: 750kg         │ │
│ │ 경로: 7개           │ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ DISP-20260127-002   │ │
│ │ [확정]              │ │
│ └─────────────────────┘ │
└─────────────────────────┘
```

### 3. 배차 상세 화면 (`DispatchDetailScreen.tsx`)

**기능**:
- 배차 정보 표시
- 경로 목록 (순서대로)
- 배차 시작/완료 버튼
- 경로별 상태 확인

**API**:
- `GET /api/v1/dispatches/{id}`
- `PUT /api/v1/dispatches/{id}/status`

**화면 구성**:
```
┌─────────────────────────┐
│ DISP-20260127-001       │
│ 2026년 01월 27일        │
├─────────────────────────┤
│ 차량  │ 주문 │ 팔레트  │
│ V001  │ 3건  │ 15개   │
├─────────────────────────┤
│ 경로 (7개)              │
│                         │
│ [1] 출발 - 차고지       │
│                         │
│ [2] 상차 - 거래처A      │
│     서울시 강남구...    │
│     팔레트: 5개         │
│                         │
│ [3] 하차 - 거래처B      │
│     인천시 남동구...    │
│     팔레트: 0개         │
│                         │
├─────────────────────────┤
│   [배차 시작]           │
└─────────────────────────┘
```

## 🔐 인증 플로우

```
1. 로그인 화면
   ↓ (username, password)
2. API 호출: POST /auth/login
   ↓ (access_token, user)
3. AsyncStorage에 토큰 저장
   ↓
4. AuthContext 업데이트
   ↓
5. 홈 화면으로 이동
   ↓
6. 모든 API 요청에 토큰 자동 첨부
```

## 📡 API 통신

### API 서비스 (`api.ts`)

**기능**:
- Axios 인스턴스 생성
- Request 인터셉터 (토큰 주입)
- Response 인터셉터 (에러 처리)
- 401 에러 시 자동 로그아웃

**사용 예시**:
```typescript
import ApiService from './services/api';

// 로그인
const response = await ApiService.login('driver1', 'password');

// 배차 조회
const dispatches = await ApiService.getMyDispatches('2026-01-27');

// 배차 상태 업데이트
await ApiService.updateDispatchStatus(1, 'IN_PROGRESS');

// 위치 업데이트
await ApiService.updateLocation(37.5665, 126.9780);

// 사진 업로드
await ApiService.uploadPhoto(1, 2, 'pickup', 'file://...');
```

## 🎨 UI/UX 디자인

### 색상 팔레트

```typescript
const colors = {
  primary: '#007AFF',      // iOS 블루
  success: '#34C759',      // 초록 (완료)
  warning: '#FF9500',      // 주황 (진행중)
  danger: '#FF3B30',       // 빨강 (취소)
  gray: '#666',           // 회색 (차고지)
  background: '#f5f5f5',  // 배경
  card: '#fff',           // 카드
  text: '#333',           // 텍스트
  textSecondary: '#666',  // 보조 텍스트
  border: '#e0e0e0',      // 테두리
};
```

### 타이포그래피

```typescript
const typography = {
  title: { fontSize: 24, fontWeight: 'bold' },
  subtitle: { fontSize: 18, fontWeight: '600' },
  body: { fontSize: 16 },
  caption: { fontSize: 14 },
  small: { fontSize: 12 },
};
```

## 🧪 테스트 (진행 예정)

```bash
# 유닛 테스트
npm test

# E2E 테스트 (Detox)
npm run test:e2e
```

## 📦 빌드 (진행 예정)

### Android

```bash
# Debug APK
cd android && ./gradlew assembleDebug

# Release APK
cd android && ./gradlew assembleRelease
```

### iOS

```bash
# Archive 생성
xcodebuild -workspace ios/ColdChainApp.xcworkspace \
  -scheme ColdChainApp \
  -configuration Release \
  -archivePath build/ColdChainApp.xcarchive \
  archive
```

## 🔧 환경 설정

### .env 파일

```env
API_BASE_URL=http://localhost:8000/api/v1
API_BASE_URL_PROD=https://api.coldchain.com/api/v1
GOOGLE_MAPS_API_KEY=your_google_maps_key
```

## 📊 현재 진행 상황

### 완료 (40%)
- [x] 프로젝트 구조 설계
- [x] TypeScript 설정
- [x] 패키지 의존성 설정
- [x] API 서비스 구현
- [x] 타입 정의
- [x] 인증 컨텍스트
- [x] 로그인 화면
- [x] 홈 화면
- [x] 배차 상세 화면

### 진행 중 (20%)
- [ ] 경로 상세 화면
- [ ] 네비게이션 설정
- [ ] 상태 관리 개선

### 예정 (40%)
- [ ] 카메라 기능
- [ ] GPS 위치 추적
- [ ] 푸시 알림
- [ ] 오프라인 지원
- [ ] 지도 통합
- [ ] 성능 최적화
- [ ] 테스트 작성
- [ ] 빌드 설정

## 🔜 다음 단계

1. **경로 상세 화면 완성** (1일)
   - 도착/출발 체크
   - 사진 촬영 버튼
   - 메모 입력

2. **카메라 기능 구현** (2일)
   - 사진 촬영
   - 미리보기
   - 업로드

3. **GPS 위치 추적** (2일)
   - 백그라운드 서비스
   - 위치 전송
   - 배터리 최적화

4. **네비게이션 완성** (1일)
   - Stack Navigator
   - Tab Navigator
   - 딥링크 지원

5. **푸시 알림** (2일)
   - Firebase 설정
   - 알림 수신
   - 알림 처리

6. **테스트 및 최적화** (3일)
   - 유닛 테스트
   - E2E 테스트
   - 성능 최적화

**예상 완료**: 2026-02-10 (2주 소요)

## 📞 문의

- **GitHub**: https://github.com/rpaakdi1-spec/3-
- **문서**: `mobile-app/README.md`

---

**기사용 모바일 앱 개발이 시작되었습니다!** 📱  
**Created with ❤️ for Cold Chain Logistics**  
**Date**: 2026-01-27
