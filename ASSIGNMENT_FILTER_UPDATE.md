# 차량-운전자 배정 관리 개선 완료

**작성일**: 2026-02-27  
**커밋**: 16254f1  
**상태**: ✅ 완료 및 배포 준비

---

## 🎯 추가된 기능

### 배정 상태 필터 (신규!)

**위치**: 필터 섹션 (검색 바 아래)

**옵션**:
- **전체**: 모든 차량 표시
- **운전자 배정됨**: 운전자가 배정된 차량만 표시
- **운전자 배정 안됨**: 운전자가 배정되지 않은 차량만 표시

**사용 사례**:
1. **배정 필요 차량 찾기**: "운전자 배정 안됨" 선택 → 운전자가 필요한 차량만 표시
2. **배정 완료 차량 확인**: "운전자 배정됨" 선택 → 이미 배정된 차량만 표시
3. **복합 필터링**: 다른 필터와 조합하여 사용
   - 예: "냉동" + "운행가능" + "운전자 배정 안됨" → 운전자가 필요한 운행 가능한 냉동 차량

---

## 📊 차량 정보 (냉동, 5톤) 수정 방법

### 차량 정보 출처
차량 카드에 표시되는 정보는 **차량관리 페이지**에서 관리됩니다:
- **차량유형** (`vehicle_type`): 냉동, 냉장, 겸용, 상온
- **톤수** (`tonnage`): 1.5톤, 5톤, 11톤 등
- **상태** (`status`): 운행가능, 운행중, 정비중, 운행불가

### 수정 방법

#### 1단계: 차량관리 페이지 접속
```
URL: http://139.150.11.99/vehicles
메뉴: 좌측 사이드바 → "차량관리" 클릭
```

#### 2단계: 차량 찾기
```
검색 기능 사용:
- 차량번호로 검색 (예: 12가3456)
- 또는 목록에서 직접 찾기
```

#### 3단계: 수정
```
1. 해당 차량의 "편집" 버튼 클릭
2. 모달 창에서 정보 수정:
   - 차량유형: 드롭다운에서 선택 (냉동/냉장/겸용/상온)
   - 톤수: 숫자 입력 (예: 1.5, 5, 11)
   - 상태: 드롭다운에서 선택
3. "저장" 버튼 클릭
```

#### 4단계: 확인
```
차량-운전자 배정 관리 페이지로 돌아가서:
- 수정된 정보 확인
- 차량 카드에 새로운 정보 표시 확인
```

---

## 🔧 기술 구현

### 배정 상태 필터 로직

```typescript
// State 추가
const [assignmentFilter, setAssignmentFilter] = useState<string>('all');

// 필터링 로직
const filteredVehicles = vehicles.filter(vehicle => {
  // ... 기존 필터들 ...
  
  // Assignment filter
  const matchesAssignment = assignmentFilter === 'all' ||
    (assignmentFilter === 'assigned' && vehicle.assigned_driver) ||
    (assignmentFilter === 'unassigned' && !vehicle.assigned_driver);
  
  return matchesSearch && matchesStatus && matchesType && 
         matchesLicense && matchesAssignment;
});
```

### UI 컴포넌트

```tsx
{/* Assignment Filter */}
<div className="flex items-center gap-2">
  <label className="text-sm font-medium text-gray-700">배정 상태:</label>
  <select
    value={assignmentFilter}
    onChange={(e) => setAssignmentFilter(e.target.value)}
    className="px-3 py-1.5 border border-gray-300 rounded-lg text-sm focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
  >
    <option value="all">전체</option>
    <option value="assigned">운전자 배정됨</option>
    <option value="unassigned">운전자 배정 안됨</option>
  </select>
</div>
```

---

## 📱 사용 가이드

### 시나리오 1: 배정 필요 차량 찾기
```
목적: 운전자가 필요한 차량만 보기

단계:
1. 배정 상태 필터 → "운전자 배정 안됨" 선택
2. 결과: 운전자가 없는 차량만 표시
3. 좌측 운전자 풀에서 운전자를 선택하여 배정
```

### 시나리오 2: 특정 조건의 미배정 차량 찾기
```
목적: 냉동 차량 중 운전자가 필요한 차량 찾기

단계:
1. 차량유형 필터 → "냉동" 선택
2. 배정 상태 필터 → "운전자 배정 안됨" 선택
3. 결과: 운전자가 없는 냉동 차량만 표시
```

### 시나리오 3: 배정 완료 차량 확인
```
목적: 이미 운전자가 배정된 차량만 보기

단계:
1. 배정 상태 필터 → "운전자 배정됨" 선택
2. 결과: 운전자가 배정된 차량만 표시
3. 필요시 X 버튼으로 배정 해제 또는 다른 차량으로 이동
```

### 시나리오 4: 복합 필터링
```
목적: 1종 대형 면허가 필요한 미배정 운행가능 차량 찾기

단계:
1. 상태 필터 → "운행가능"
2. 면허 필터 → "1종 대형"
3. 배정 상태 필터 → "운전자 배정 안됨"
4. 결과: 조건을 모두 만족하는 차량만 표시
```

---

## 🎨 UI 개선 사항

### 필터 섹션 레이아웃
```
┌─────────────────────────────────────────────────────┐
│ 🔍 검색창                           [새로고침 버튼]  │
├─────────────────────────────────────────────────────┤
│ 상태: [전체▼]  차량유형: [전체▼]                    │
│ 면허: [전체▼]  배정상태: [전체▼]  [필터 초기화]    │
└─────────────────────────────────────────────────────┘
```

### 필터 조합 예시
| 필터 조합 | 결과 |
|----------|------|
| 배정상태: 운전자 배정 안됨 | 미배정 차량 전체 |
| 배정상태: 운전자 배정됨 | 배정 완료 차량 전체 |
| 냉동 + 배정 안됨 | 운전자 필요한 냉동 차량 |
| 운행가능 + 배정 안됨 | 즉시 배정 가능 차량 |
| 1종 대형 + 배정됨 | 1종 대형 면허 운전자 배정 차량 |

---

## 📊 필터 기능 요약

### 전체 필터 목록 (5개)
1. **상태 필터**: 운행가능/운행중/정비중/운행불가
2. **차량유형 필터**: 냉동/냉장/겸용/상온
3. **운전자 면허 필터**: 1종 대형/1종 보통/2종
4. **배정 상태 필터**: 운전자 배정됨/운전자 배정 안됨 ⭐ 신규!
5. **검색**: 차량번호/운전자명/차량유형

### 필터 초기화
- 활성화된 필터가 하나라도 있으면 "필터 초기화" 버튼 자동 표시
- 버튼 클릭 시 모든 필터 + 검색어 초기화

---

## 🚀 배포 방법

### 서버 배포
```bash
# 서버 접속
ssh root@139.150.11.99

# 배포
cd /root/uvis
git pull origin main
docker-compose down frontend
docker-compose up -d --build frontend

# 확인
docker-compose ps
docker-compose logs -f frontend
```

### 접속 및 테스트
```
URL: http://139.150.11.99/vehicle-driver-management
로그인: admin / admin123

테스트:
1. 배정 상태 필터 확인
2. "운전자 배정 안됨" 선택 → 미배정 차량만 표시
3. "운전자 배정됨" 선택 → 배정된 차량만 표시
4. 필터 초기화 버튼 동작 확인
```

---

## ✅ 테스트 체크리스트

### 배정 상태 필터
- [ ] "전체" 선택 → 모든 차량 표시
- [ ] "운전자 배정됨" 선택 → 배정된 차량만 표시
- [ ] "운전자 배정 안됨" 선택 → 미배정 차량만 표시
- [ ] 다른 필터와 복합 사용 가능
- [ ] 필터 초기화 버튼에 포함됨

### 차량 정보 수정
- [ ] 차량관리 페이지 접속
- [ ] 차량 편집 모달 열기
- [ ] 차량유형 수정 (냉동/냉장/겸용/상온)
- [ ] 톤수 수정 (숫자 입력)
- [ ] 저장 후 배정 관리 페이지에서 확인

---

## 📝 차량 정보 필드 상세

### Vehicle 모델 (backend/app/models/vehicle.py)
```python
class Vehicle(Base):
    id: int
    code: str              # 차량 코드
    plate_number: str      # 차량번호 (12가3456)
    vehicle_type: str      # 차량유형 (냉동/냉장/겸용/상온)
    tonnage: float         # 톤수 (1.5, 5, 11)
    status: str            # 상태 (운행가능/운행중/정비중/운행불가)
    driver_name: str       # 운전자명 (nullable)
    driver_phone: str      # 운전자 전화번호 (nullable)
    # ... 기타 필드들
```

### 수정 가능한 필드
- ✅ **차량유형** (vehicle_type): 배정 관리 페이지에 표시
- ✅ **톤수** (tonnage): 배정 관리 페이지에 표시
- ✅ **상태** (status): 필터 및 배지로 표시
- ✅ **운전자명/전화번호**: 배정 시 자동 업데이트

### 수정 방법
1. **차량관리 페이지** → 편집 → 저장
2. **API 직접 호출** (개발자용):
   ```bash
   curl -X PUT http://139.150.11.99/api/v1/vehicles/{id} \
     -H "Content-Type: application/json" \
     -d '{"vehicle_type": "냉동", "tonnage": 5}'
   ```

---

## 🎉 결론

### 추가된 기능
✅ **배정 상태 필터**: 운전자 배정 여부로 차량 필터링

### 개선 효과
- **빠른 검색**: 미배정 차량 즉시 확인
- **효율성**: 배정 필요 차량에 집중
- **복합 필터링**: 다른 필터와 조합 가능

### 차량 정보 관리
- **수정 위치**: 차량관리 페이지 (/vehicles)
- **수정 필드**: 차량유형, 톤수, 상태 등
- **반영**: 즉시 배정 관리 페이지에 표시

---

## 📚 관련 페이지

1. **차량-운전자 배정 관리** (현재 페이지)
   - URL: /vehicle-driver-management
   - 기능: 드래그앤드롭 배정, X 버튼 해제, 필터

2. **차량관리**
   - URL: /vehicles
   - 기능: 차량 정보 수정 (차량유형, 톤수, 상태 등)

3. **운전자 관리** (향후 개발 예정)
   - 운전자 정보 관리
   - 면허, 근무시간 등 수정

---

**커밋**: 16254f1  
**배포 준비**: ✅ 완료  
**배포 URL**: http://139.150.11.99/vehicle-driver-management  
**로그인**: admin / admin123
