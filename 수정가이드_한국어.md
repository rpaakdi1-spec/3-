# UVIS Dashboard 에러 수정 가이드 (한국어)

## 📋 현재 상태

### ✅ 작동하는 기능
- 로그인 (admin/admin123)
- 대시보드 페이지 표시
- 통계 API (/api/v1/dispatches/stats/summary)
- 폴링 방식 업데이트 (5초마다)

### ❌ 문제점
1. **차량 API 307 리다이렉트**: `/api/v1/vehicles` → `/api/v1/vehicles/` 로 리다이렉트
2. **WebSocket 403 에러**: 실시간 연결 실패
3. **백엔드 비동기 에러**: `ChunkedIteratorResult` 관련 오류

---

## 🛠️ 수정 방법

### 방법 1: 자동 스크립트 실행 (권장)

```bash
# 서버에 SSH 접속 후

# 1. 차량 API 리다이렉트 수정
cd /root/uvis/frontend
./fix_vehicle_api_redirect.sh

# 2. 모든 API 테스트
./test_all_apis.sh

# 3. 백엔드 에러 진단 (필요시)
./diagnose_async_errors.sh
```

### 방법 2: 수동 수정

#### A. 차량 API 307 리다이렉트 수정

```bash
cd /root/uvis/frontend

# 백업
cp nginx.conf nginx.conf.backup.$(date +%Y%m%d_%H%M%S)

# nginx.conf 편집 (location /api/v1/ws/ 앞에 추가)
vi nginx.conf

# 아래 내용을 추가:
        # Fix vehicle API redirect
        rewrite ^/api/v1/vehicles$ /api/v1/vehicles/ permanent;

# 배포
docker cp nginx.conf uvis-frontend:/etc/nginx/nginx.conf
docker exec uvis-frontend nginx -t
docker exec uvis-frontend nginx -s reload
```

#### B. WebSocket 인증 수정 (백엔드 코드 변경 필요)

```bash
# 1. WebSocket 구현 확인
docker exec uvis-backend cat /app/app/api/v1/websocket.py | head -50

# 2. 토큰 검증 로직이 없다면 추가 필요:
# - Query parameter에서 token 읽기
# - JWT 토큰 검증
# - 실패시 WebSocket 연결 거부

# 예시 코드는 BACKEND_FIXES_GUIDE.md 참조
```

#### C. 비동기 에러 수정 (백엔드 코드 변경 필요)

```bash
# 1. 에러 위치 찾기
docker exec uvis-backend grep -rn "broadcast.*vehicle" /app/app --include="*.py"

# 2. 잘못된 패턴:
#    result = await session.execute(select(...))
#    await result  # ❌ 오류

# 3. 올바른 패턴:
#    result = await session.execute(select(...))
#    items = result.scalars().all()  # ✅ 정상
```

---

## 🧪 테스트 방법

### 1. API 테스트 (터미널)

```bash
# 토큰 발급
TOKEN=$(curl -s -X POST http://139.150.11.99/api/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123" | \
  python3 -c "import sys,json;print(json.load(sys.stdin)['access_token'])")

# 차량 API 테스트 (307 아닌 200 응답이어야 함)
curl -X GET "http://139.150.11.99/api/v1/vehicles" \
  -H "Authorization: Bearer $TOKEN" -v

# 대시보드 통계 테스트
curl -X GET "http://139.150.11.99/api/v1/dispatches/stats/summary" \
  -H "Authorization: Bearer $TOKEN" | python3 -m json.tool
```

### 2. 브라우저 테스트

```
1. 브라우저 열기 (Chrome 권장)
2. F12 → Network 탭 활성화, "Disable cache" 체크
3. http://139.150.11.99 접속
4. admin / admin123 로그인
5. Console 탭 확인:
   - ✅ "WebSocket connected: dashboard" 메시지
   - ❌ 403 에러 없어야 함
6. Network 탭 확인:
   - WS 연결이 "101 Switching Protocols" 상태
   - 차량 API가 200 OK (307 아님)
```

---

## 📊 예상 결과

### 수정 전
```
GET /api/v1/vehicles → 307 Temporary Redirect
WS /api/v1/ws/dashboard → 403 Forbidden
Backend logs → Error broadcasting vehicle updates
```

### 수정 후
```
GET /api/v1/vehicles → 200 OK + 차량 목록 JSON
WS /api/v1/ws/dashboard → 101 Switching Protocols
Backend logs → No errors (또는 최소한의 에러)
Dashboard → 실시간 업데이트 (녹색 점 표시)
```

---

## 🚨 주의사항

### 1. 백엔드 코드 수정시

백엔드 코드(Python)를 수정한 경우 **반드시 컨테이너 재시작**:

```bash
docker restart uvis-backend

# 로그 확인 (30초 대기)
sleep 30
docker logs uvis-backend --tail 50
```

### 2. 프론트엔드 수정시

프론트엔드 코드를 수정한 경우:

```bash
cd /root/uvis/frontend
npm run build
cd /root/uvis
docker cp frontend/dist/. uvis-frontend:/usr/share/nginx/html/
docker exec uvis-frontend nginx -s reload

# 브라우저에서 강제 새로고침: Ctrl + Shift + F5
```

### 3. Nginx 설정 수정시

```bash
# 항상 테스트 먼저
docker exec uvis-frontend nginx -t

# 성공하면 리로드
docker exec uvis-frontend nginx -s reload
```

---

## 🔍 문제 해결

### Q1: 차량 API가 여전히 307을 반환합니다

**A1:** Nginx 설정이 올바르게 적용되지 않았을 수 있습니다.

```bash
# 현재 설정 확인
docker exec uvis-frontend cat /etc/nginx/nginx.conf | grep -A 5 "rewrite.*vehicles"

# 설정이 없다면 다시 적용
cd /root/uvis/frontend
docker cp nginx.conf uvis-frontend:/etc/nginx/nginx.conf
docker exec uvis-frontend nginx -s reload
```

### Q2: WebSocket이 여전히 403을 반환합니다

**A2:** 백엔드에서 토큰 검증이 구현되지 않았습니다. 두 가지 옵션:

1. **임시 해결책**: WebSocket 없이 폴링만 사용 (현재 상태)
2. **완전 해결**: 백엔드 코드 수정 필요 (websocket.py 파일)

### Q3: "ChunkedIteratorResult" 에러가 계속 나옵니다

**A3:** SQLAlchemy 비동기 쿼리 사용법 문제입니다.

```bash
# 에러 위치 찾기
docker exec uvis-backend grep -rn "broadcast" /app/app/api -A 10 | grep -B 5 "await result"

# 해당 파일을 찾아서 수정:
# result = await db.execute(...)
# items = result.scalars().all()  # 이 줄 추가
```

---

## 📚 참고 자료

### 생성된 파일들

이 가이드와 함께 생성된 파일들:

1. **BACKEND_FIXES_GUIDE.md** - 상세한 영문 수정 가이드
2. **fix_vehicle_api_redirect.sh** - 차량 API 리다이렉트 자동 수정 스크립트
3. **diagnose_async_errors.sh** - 백엔드 비동기 에러 진단 스크립트
4. **test_all_apis.sh** - 전체 API 테스트 스크립트

### 관련 문서

- **COMPLETE_FIX_RECORD.md** - 이전까지의 수정 기록
- **DASHBOARD_422_FIX.md** - 대시보드 422 에러 수정 기록
- **FINAL_FIX_SUMMARY.md** - 전체 수정 요약

---

## ✅ 완료 체크리스트

수정 완료 후 아래 항목들을 확인하세요:

- [ ] 로그인 성공 (admin/admin123)
- [ ] 대시보드 페이지 로드
- [ ] 차량 API 200 OK 응답 (307 아님)
- [ ] 대시보드 통계 표시 (0개라도 정상)
- [ ] WebSocket 연결 (403 에러 없음)
- [ ] Console에 "WebSocket connected" 메시지
- [ ] 백엔드 로그에 에러 없음
- [ ] 실시간 업데이트 표시 (녹색 점)

모든 항목이 체크되면 시스템이 완전히 작동하는 것입니다!

---

## 🎯 우선순위

1. **즉시 수정**: 차량 API 307 리다이렉트 (1분)
2. **높음**: 백엔드 비동기 에러 (코드 수정 필요)
3. **중간**: WebSocket 토큰 인증 (코드 수정 필요)

**추천 순서**:
1. `./fix_vehicle_api_redirect.sh` 실행
2. `./test_all_apis.sh` 실행하여 결과 확인
3. 필요시 백엔드 코드 수정
4. 브라우저에서 최종 테스트

---

**문의사항이나 추가 도움이 필요하면 알려주세요!** 🚀
