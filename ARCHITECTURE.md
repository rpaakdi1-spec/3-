# 시스템 아키텍처 (System Architecture)

## 📐 전체 구조

```
┌─────────────────────────────────────────────────────────────────┐
│                         사용자 인터페이스                          │
│                    (React Frontend - 예정)                       │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐        │
│  │ 거래처 관리 │  │ 차량 관리 │  │ 주문 관리 │  │ 배차 관리 │        │
│  └──────────┘  └──────────┘  └──────────┘  └──────────┘        │
└─────────────────────┬───────────────────────────────────────────┘
                      │ REST API (JSON)
┌─────────────────────┴───────────────────────────────────────────┐
│                      FastAPI Backend                            │
│  ┌────────────────────────────────────────────────────────┐    │
│  │                    API Layer                            │    │
│  │  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐    │    │
│  │  │Clients│  │Vehicles│ │Drivers│ │Orders│  │Dispatch│    │    │
│  │  │  API  │  │  API   │ │  API  │ │  API │  │  API   │    │    │
│  │  └───┬──┘  └───┬───┘ └───┬──┘  └───┬──┘  └───┬───┘    │    │
│  └──────┼─────────┼──────────┼─────────┼─────────┼────────┘    │
│         │         │          │         │         │              │
│  ┌──────┴─────────┴──────────┴─────────┴─────────┴────────┐    │
│  │                  Service Layer                          │    │
│  │  ┌─────────────────┐  ┌──────────────────────────┐    │    │
│  │  │ Excel Template  │  │   Naver Map Service      │    │    │
│  │  │    Service      │  │  - Geocoding             │    │    │
│  │  │  - Create       │  │  - Routing               │    │    │
│  │  │  - Parse        │  │  - Distance Calculation  │    │    │
│  │  └─────────────────┘  └──────────────────────────┘    │    │
│  │  ┌─────────────────────────────────────────────────┐  │    │
│  │  │     AI Optimization Service (예정)               │  │    │
│  │  │  - Google OR-Tools VRP                          │  │    │
│  │  │  - Hard Constraints Validation                  │  │    │
│  │  │  - Soft Constraints Optimization                │  │    │
│  │  └─────────────────────────────────────────────────┘  │    │
│  │  ┌─────────────────────────────────────────────────┐  │    │
│  │  │      UVIS Integration Service (예정)             │  │    │
│  │  │  - Real-time GPS Tracking                       │  │    │
│  │  │  - Temperature Monitoring                       │  │    │
│  │  └─────────────────────────────────────────────────┘  │    │
│  └─────────────────────┬───────────────────────────────┘    │
└────────────────────────┼─────────────────────────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │                               │
    ┌────┴────┐                    ┌────┴────┐
    │SQLAlchemy│                    │  Redis  │
    │   ORM   │                    │  Cache  │
    └────┬────┘                    └─────────┘
         │
    ┌────┴────┐
    │SQLite DB│
    │(Dev)    │
    │PostgreSQL│
    │(Prod)   │
    └─────────┘
```

## 🔄 데이터 흐름

### 1. 거래처 등록 + 지오코딩
```
엑셀 업로드 → Parse Excel → Validate Data → Save to DB → Naver Geocode API → Update Coordinates
```

### 2. 주문 등록
```
주문 입력 → Validate Clients → Check Temperature Zone → Save Order → Status: 배차대기
```

### 3. AI 배차 생성 (예정)
```
주문 목록 + 차량 목록 → OR-Tools VRP
                           ↓
        Hard Constraints Check (온도대, 용량, 시간)
                           ↓
        Soft Optimization (거리, 공차율, 균형)
                           ↓
        배차 계획 생성 → 경로 순서 결정 → 저장
```

### 4. 실시간 모니터링 (예정)
```
UVIS API → GPS 좌표 → Redis Cache → WebSocket → Frontend 실시간 업데이트
```

## 🗄️ 데이터베이스 ERD

```
┌──────────────────┐          ┌──────────────────┐
│     clients      │          │     vehicles     │
├──────────────────┤          ├──────────────────┤
│ PK id            │          │ PK id            │
│    code (UK)     │          │    code (UK)     │
│    name          │          │    plate_number  │
│    client_type   │          │    vehicle_type  │
│    latitude      │          │    max_pallets   │
│    longitude     │          │    max_weight_kg │
│    ...           │          │    ...           │
└────────┬─────────┘          └────────┬─────────┘
         │                              │
         │  ┌──────────────────┐       │
         │  │     drivers      │       │
         │  ├──────────────────┤       │
         │  │ PK id            │       │
         │  │    code (UK)     │       │
         │  │    name          │       │
         │  │    phone         │       │
         │  │    work_hours    │       │
         │  └────────┬─────────┘       │
         │           │                 │
    ┌────┴───────────┴─────────────────┴────┐
    │            orders                      │
    ├────────────────────────────────────────┤
    │ PK id                                  │
    │    order_number (UK)                   │
    │ FK pickup_client_id                    │
    │ FK delivery_client_id                  │
    │    temperature_zone                    │
    │    pallet_count                        │
    │    weight_kg                           │
    │    status                              │
    └────────┬───────────────────────────────┘
             │
    ┌────────┴───────────────────────────────┐
    │           dispatches                   │
    ├────────────────────────────────────────┤
    │ PK id                                  │
    │    dispatch_number (UK)                │
    │ FK vehicle_id                          │
    │ FK driver_id                           │
    │    dispatch_date                       │
    │    total_distance_km                   │
    │    optimization_score                  │
    └────────┬───────────────────────────────┘
             │
    ┌────────┴───────────────────────────────┐
    │        dispatch_routes                 │
    ├────────────────────────────────────────┤
    │ PK id                                  │
    │ FK dispatch_id                         │
    │ FK order_id                            │
    │    sequence                            │
    │    route_type                          │
    │    estimated_arrival_time              │
    │    current_pallets                     │
    └────────────────────────────────────────┘
```

## 🎯 핵심 비즈니스 로직

### 배차 최적화 제약 조건

#### Hard Constraints (반드시 준수)
```python
1. 온도대 매칭
   if order.temperature_zone == "냉동":
       vehicle.vehicle_type in ["냉동", "겸용"]

2. 용량 제약
   sum(order.pallet_count) <= vehicle.max_pallets
   sum(order.weight_kg) <= vehicle.max_weight_kg

3. 타임 윈도우
   pickup_time in [client.pickup_start, client.pickup_end]
   delivery_time in [client.delivery_start, client.delivery_end]

4. 기사 근무시간
   total_work_time <= driver.max_work_hours

5. 지게차 필요
   if order.requires_forklift:
       client.has_forklift == True
```

#### Soft Constraints (최적화 목표)
```python
1. 공차거리 최소화
   minimize: distance(garage → first_pickup)
   minimize: distance(last_delivery → garage)

2. 총 주행거리 최소화
   minimize: total_distance

3. 차량/기사 업무 균형
   variance(orders_per_vehicle) → 0
   variance(distance_per_vehicle) → 0
```

## 🔌 외부 시스템 연동

### 1. Naver Map API
```
Geocoding API:
POST https://naveropenapi.apigw.ntruss.com/map-geocode/v2/geocode
Headers:
  X-NCP-APIGW-API-KEY-ID: {client_id}
  X-NCP-APIGW-API-KEY: {client_secret}

Directions API:
GET https://naveropenapi.apigw.ntruss.com/map-direction/v1/driving
Params:
  start: {longitude},{latitude}
  goal: {longitude},{latitude}
```

### 2. Samsung UVIS API (예정)
```
GPS Tracking:
GET https://api.s1.co.kr/uvis/v1/vehicles/{device_id}/location
Headers:
  Authorization: Bearer {api_key}

Response:
{
  "latitude": 37.1234,
  "longitude": 127.5678,
  "temperature": -20.5,
  "timestamp": "2026-01-19T10:30:00Z"
}
```

## 🚀 배포 아키텍처 (프로덕션)

```
                      ┌──────────────┐
                      │   Cloudflare │
                      │      CDN     │
                      └──────┬───────┘
                             │
                      ┌──────┴───────┐
                      │  Load Balancer│
                      └──────┬───────┘
                             │
            ┌────────────────┼────────────────┐
            │                │                │
      ┌─────┴─────┐   ┌─────┴─────┐   ┌─────┴─────┐
      │ FastAPI   │   │ FastAPI   │   │ FastAPI   │
      │ Instance 1│   │ Instance 2│   │ Instance 3│
      └─────┬─────┘   └─────┬─────┘   └─────┬─────┘
            │                │                │
            └────────────────┼────────────────┘
                             │
                      ┌──────┴───────┐
                      │  PostgreSQL  │
                      │  Primary DB  │
                      └──────┬───────┘
                             │
                      ┌──────┴───────┐
                      │  Redis       │
                      │  Cluster     │
                      └──────────────┘
```

## 📊 성능 목표

- **API 응답 시간**: < 200ms (평균)
- **배차 최적화**: < 5초 (40대, 110건 기준)
- **동시 사용자**: 50명
- **데이터베이스**: 10,000+ 주문/일
- **GPS 업데이트**: 10-30초 간격

## 🔒 보안 계층

```
1. API Gateway
   - Rate Limiting
   - CORS 설정
   - API Key 검증

2. Application Layer
   - JWT 인증
   - Role-based Access Control
   - Input Validation

3. Database Layer
   - Prepared Statements (SQLAlchemy)
   - Encryption at Rest
   - Access Control

4. Network Layer
   - HTTPS/TLS 1.3
   - VPN for UVIS connection
```

---

**Last Updated**: 2026-01-19
