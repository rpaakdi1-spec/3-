# 템플릿 관리 페이지 사용 방법

## 🚀 빠른 시작 (3분)

### 1️⃣ 시크릿 모드로 접속 (가장 빠름!)

```
1. Ctrl + Shift + N (Chrome) 또는 Ctrl + Shift + P (Firefox)
2. http://139.150.11.99 접속
3. 로그인:
   - Username: admin
   - Password: (관리자 비밀번호)
4. 사이드바 → 운영 관리 → 📄 템플릿 관리 (NEW) 클릭
```

**✅ 이 방법이 성공하면** → 일반 브라우저에서 캐시만 삭제하면 됩니다

---

### 2️⃣ 일반 브라우저에서 캐시 삭제

시크릿 모드가 잘 작동했다면:

```
1. 일반 브라우저에서 Ctrl + Shift + Delete
2. "전체 기간" 선택
3. "쿠키" 체크 + "캐시" 체크
4. 데이터 삭제
5. 페이지 새로고침 (F5)
6. 로그아웃 → 재로그인
```

---

## 📱 템플릿 관리 기능

### 템플릿 목록 화면

```
┌─────────────────────────────────────────────────────┐
│  템플릿 관리                          [+ 새 템플릿] │
├─────────────────────────────────────────────────────┤
│  🔍 검색: [____________]  고객: [전체▾]  정렬: [▾] │
├─────────────────────────────────────────────────────┤
│  📊 통계                                            │
│  전체: 9개  │  활성: 5개  │  즐겨찾기: 2개          │
├─────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────┐  │
│  │ ⭐ 이천배차 (월~토)                  동원     │  │
│  │ 12건 배차 │ 13회 사용 │ ✅ 활성              │  │
│  │ [⚡] [✏️] [📋] [🗑️]                          │  │
│  └──────────────────────────────────────────────┘  │
│  ┌──────────────────────────────────────────────┐  │
│  │ ☆ 도미노 백암 → 밀양               도미노 백암│  │
│  │ 8건 배차 │ 83회 사용 │ ✅ 활성               │  │
│  │ [⚡] [✏️] [📋] [🗑️]                          │  │
│  └──────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────┘
```

### 기능 설명

| 아이콘 | 기능 | 설명 |
|--------|------|------|
| ⭐ | 즐겨찾기 | 클릭하면 즐겨찾기 추가/제거 |
| ⚡ | 활성화 | 클릭하면 활성/비활성 전환 |
| ✏️ | 수정 | 템플릿 수정 (구현 예정) |
| 📋 | 복제 | 템플릿 복사본 생성 |
| 🗑️ | 삭제 | 확인 후 삭제 |

---

## ✅ 기능 테스트

### 1. 즐겨찾기 추가

```
1. 템플릿 카드에서 ⭐ 아이콘 클릭
2. 토스트 메시지: "즐겨찾기에 추가했습니다" 표시
3. 별이 노란색으로 변경
4. 통계에서 "즐겨찾기" 카운트 증가
```

**확인**:
- 상단 통계 카운트 변경됨
- 별 아이콘 색상 변경됨

### 2. 템플릿 복제

```
1. 템플릿 카드에서 📋 아이콘 클릭
2. 토스트 메시지: "템플릿을 복제했습니다" 표시
3. 목록 상단에 "(복사본)" 템플릿 추가됨
```

**확인**:
- 새 템플릿: "이천배차 (월~토) (복사본)"
- 전체 템플릿 개수 증가

### 3. 활성화/비활성화

```
1. 템플릿 카드에서 ⚡ 아이콘 클릭
2. 토스트 메시지 표시
3. "비활성" 배지 표시/숨김
```

**확인**:
- 활성 템플릿 카운트 변경
- 배지 표시 변경

### 4. 템플릿 삭제

```
1. 템플릿 카드에서 🗑️ 아이콘 클릭
2. 확인 다이얼로그: "템플릿을 삭제하시겠습니까?"
3. 확인 클릭
4. 토스트 메시지: "템플릿을 삭제했습니다" 표시
5. 목록에서 제거됨
```

**확인**:
- 전체 템플릿 개수 감소
- 해당 템플릿 사라짐

### 5. 검색

```
1. 검색창에 "이천" 입력
2. "이천배차" 템플릿만 표시됨

또는

1. 검색창에 "동원" 입력
2. 고객명이 "동원"인 템플릿들만 표시됨
```

### 6. 고객 필터

```
1. "고객" 드롭다운 클릭
2. "동원" 선택
3. 동원 고객 템플릿만 표시됨
```

### 7. 정렬

```
1. 정렬 드롭다운 클릭
2. "사용 횟수" 선택
3. 사용 횟수가 많은 순으로 정렬됨
```

---

## 🔍 문제 해결

### Q1. 즐겨찾기 클릭 시 아무 반응이 없어요

**A1. Network 탭 확인**

```
1. F12 → Network 탭
2. 즐겨찾기 ⭐ 클릭
3. templates/40 PUT 요청 찾기
4. Request Headers 확인:
   ✅ Authorization: Bearer ... 있음 → 정상
   ❌ Authorization 헤더 없음 → 캐시 문제
```

**해결**: 캐시 삭제 (위의 "2️⃣ 일반 브라우저에서 캐시 삭제" 참조)

---

### Q2. "401 Unauthorized" 오류가 나와요

**A2-1. 로그인 상태 확인**

```
F12 → Console 탭에서 실행:
localStorage.getItem('access_token')

결과가 null → 로그아웃 상태, 다시 로그인
```

**A2-2. 캐시 삭제 및 재로그인**

```
F12 → Console 탭에서 실행:
localStorage.clear();
location.href = '/login';
```

---

### Q3. 시크릿 모드에서는 되는데 일반 브라우저에서 안 돼요

**A3. 브라우저 캐시 문제 확정**

```
1. 일반 브라우저에서 Ctrl + Shift + Delete
2. 전체 기간 → 쿠키 + 캐시 삭제
3. F12 개발자 도구 열기
4. Network 탭 → "Disable cache" 체크
5. Ctrl + Shift + R (강력 새로고침)
6. 로그아웃 → 재로그인
```

---

### Q4. 직접 API가 정상 작동하는지 확인하고 싶어요

**A4. 테스트 페이지 사용**

```
http://139.150.11.99/test-api.html

이 페이지에서:
1. "1️⃣ 로그인 상태 확인" 클릭
2. "2️⃣ 템플릿 목록 조회" 클릭
3. "3️⃣ 즐겨찾기 변경" 클릭

모두 성공 → API 정상, 메인 앱 캐시 문제
```

---

## 📊 서버에서 DB 확인 (관리자용)

### 최근 변경된 템플릿 확인

```bash
cd /root/uvis

docker compose exec -T db psql -U uvis_user -d uvis_db -c "
SELECT id, name, is_favorite, is_active, updated_at 
FROM dispatch_form_templates 
ORDER BY updated_at DESC 
LIMIT 5;
"
```

### 즐겨찾기 템플릿 확인

```bash
docker compose exec -T db psql -U uvis_user -d uvis_db -c "
SELECT id, name, is_favorite 
FROM dispatch_form_templates 
WHERE is_favorite = true;
"
```

### 전체 통계

```bash
docker compose exec -T db psql -U uvis_user -d uvis_db -c "
SELECT 
  COUNT(*) as 전체템플릿수,
  SUM(CASE WHEN is_active THEN 1 ELSE 0 END) as 활성템플릿수,
  SUM(CASE WHEN is_favorite THEN 1 ELSE 0 END) as 즐겨찾기수,
  SUM(usage_count) as 총사용횟수
FROM dispatch_form_templates;
"
```

---

## 🎯 빠른 해결 플로우차트

```
템플릿 관리 페이지에서 기능이 안 됨
             ↓
    시크릿 모드로 테스트
             ↓
    ┌────────┴────────┐
    ↓                 ↓
  성공              실패
    ↓                 ↓
캐시 문제          토큰 문제
    ↓                 ↓
Ctrl+Shift+Delete  재로그인
    ↓
완료! ✅
```

---

## 📞 도움이 더 필요하면

다음 정보를 제공해주세요:

1. **브라우저 콘솔 스크린샷**
   ```
   F12 → Console 탭 스크린샷
   ```

2. **Network 탭 스크린샷**
   ```
   F12 → Network 탭
   → 템플릿 기능 클릭
   → Request Headers 부분 스크린샷
   ```

3. **에러 메시지**
   ```
   토스트 메시지 내용 또는 Console 에러
   ```

---

## ✨ 요약

### ✅ 정상 작동 시

- 토스트 메시지 표시
- 아이콘 변경 (별, 배지 등)
- 통계 카운트 변경
- DB에 반영

### ❌ 문제 발생 시

**가장 빠른 해결책**:
```
1. Ctrl + Shift + N (시크릿 모드)
2. 로그인
3. 템플릿 관리 페이지 테스트
4. 성공 → 일반 브라우저 캐시 삭제
```

---

**준비 완료!** 🎉

지금 바로 시크릿 모드로 `http://139.150.11.99`에 접속하여 테스트해보세요!
