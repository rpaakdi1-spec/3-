# 🚀 AI 배차 시스템 사용 가이드

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [배포 완료 확인](#배포-완료-확인)
3. [캐시 초기화 (필수)](#캐시-초기화-필수)
4. [사용 방법](#사용-방법)
5. [문제 해결](#문제-해결)

---

## 🎯 시스템 개요

### 주요 기능
- **자동 차량 검색**: 상차지 기준 150km 반경 내 대기 중인 차량 자동 검색
- **AI 추천 점수**: 거리, 상태, 가용성을 종합한 0-100점 점수 제공
- **실시간 정보**: 현재 위치, 예상 도착 시간, 거리 계산
- **사용자 최종 선택**: AI가 추천하되 사용자가 최종 결정

### 기술 스펙
- **Backend**: FastAPI (Python)
  - `GET /api/v1/semi-auto-dispatch/orders/{order_id}/suggest-vehicles`
  - `POST /api/v1/semi-auto-dispatch/orders/{order_id}/manual-dispatch`
- **Frontend**: React + TypeScript
- **지도 API**: Naver Map API (거리/시간 계산)

---

## ✅ 배포 완료 확인

### 1. 서버 상태 확인
```bash
curl http://139.150.11.99/api/v1/health
```

**예상 응답:**
```json
{
  "status": "healthy",
  "app_name": "Cold Chain Dispatch System",
  "environment": "production"
}
```

### 2. API 엔드포인트 확인
```bash
docker compose exec -T backend python -c "
from main import app
routes = [r for r in app.routes if hasattr(r,'path') and 'semi' in r.path.lower()]
for r in routes:
    print(f'✅ {r.path} - {r.methods if hasattr(r,\"methods\") else \"N/A\"}')
"
```

**예상 출력:**
```
✅ /api/v1/semi-auto-dispatch/orders/{order_id}/suggest-vehicles - {'GET'}
✅ /api/v1/semi-auto-dispatch/orders/{order_id}/manual-dispatch - {'POST'}
```

### 3. 캐시 초기화 페이지 확인
```bash
curl -I http://139.150.11.99/clear-cache.html
```

**예상 응답:** `HTTP/1.1 200 OK` + 캐시 무효화 헤더

---

## 🔧 캐시 초기화 (필수)

### ⚠️ 왜 필요한가?
- 브라우저가 **오래된 JavaScript 파일**을 캐시하여 404 오류 발생
- 이전 버전의 코드가 **존재하지 않는 API 엔드포인트**를 호출
- **새로 빌드된 파일**(새 해시값)을 로드해야 정상 작동

### 방법 1: 자동 캐시 초기화 ⭐ (가장 쉬움)

**사용자 안내:**
```
1. 브라우저에서 아래 URL 접속:
   👉 http://139.150.11.99/clear-cache.html

2. 페이지에서 "자동 캐시 초기화 및 재시작" 버튼 클릭

3. 5초 후 자동으로 메인 페이지로 이동

4. 완료!
```

### 방법 2: 시크릿 모드 🔒 (빠른 확인)

**사용자 안내:**
```
1. Ctrl + Shift + N (Chrome/Edge)
   또는 Ctrl + Shift + P (Firefox) 누르기

2. 시크릿 창에서 접속:
   http://139.150.11.99/orders

3. 로그인:
   - 이메일: manager1@test.com
   - 비밀번호: test1234

4. "AI 배차" 버튼 테스트
```

**✅ 시크릿 모드에서 정상 작동하면:**
- 일반 브라우저의 캐시 문제 확인됨
- 방법 1 또는 방법 3으로 캐시 삭제 필요

### 방법 3: 수동 캐시 삭제 (Chrome/Edge)

**사용자 안내:**
```
1. Ctrl + Shift + Delete 누르기

2. "캐시된 이미지 및 파일" 체크

3. "데이터 삭제" 클릭

4. Ctrl + Shift + R 로 강력 새로고침 (3-5회 반복)
```

### 방법 4: 개발자 도구 (고급)

**사용자 안내:**
```
1. F12 눌러 개발자 도구 열기

2. Network 탭 선택

3. "Disable cache" 체크박스 선택

4. 개발자 도구를 열어둔 상태에서 새로고침 (Ctrl+R)

5. Network 탭에서 로드된 파일 확인:
   ❌ OrdersPage-2ebTppXZ.js (오래된 캐시)
   ✅ OrdersPage-XXXXXXXX.js (새 해시 - 정상)
```

---

## 📱 사용 방법

### 전제 조건
- **주문 상태**: "배차대기" 상태여야 함
- **사용자 권한**: MANAGER, VEHICLE_MANAGER, ADMIN 중 하나
- **로그인 계정**: manager1@test.com / test1234 (테스트용)

### Step 1: 주문 페이지 접속
```
http://139.150.11.99/orders
```

### Step 2: 배차대기 주문 찾기

**옵션 A: 기존 주문 사용**
- 주문 목록에서 상태가 "배차대기"인 주문 찾기

**옵션 B: 새 주문 생성**
```
1. "+" 버튼 클릭
2. 주문 정보 입력:
   - 주문번호: TEST-001
   - 상차지: 서울 강남구 테헤란로 123
   - 하차지: 부산 해운대구 해운대로 456
   - 온도 범위: -18°C ~ -15°C
   - 팔레트 수: 10개
3. 저장
```

**옵션 C: AI 어시스턴트로 생성**
```
1. "AI 어시스턴트" 탭 선택
2. 입력: "서울 강남구에서 부산 해운대구로 냉동 10팔레트 배송"
3. "확인" 클릭
4. 자동으로 주문 생성 (상태: 배차대기)
```

### Step 3: AI 배차 실행

**UI 요소:**
- 버튼 위치: 주문 목록의 "작업" 컬럼
- 아이콘: 🧭 파란색 Navigation 아이콘
- 툴팁: "AI 배차"

**클릭하면:**
1. 모달 창이 열림
2. 자동으로 차량 검색 시작
3. 로딩 스피너 표시

### Step 4: 추천 차량 확인

**모달 구조:**
```
┌─────────────────────────────────────────────┐
│ AI 배차 - 차량 추천                    [X] │
├─────────────────────────────────────────────┤
│ 🔍 검색 조건                                │
│   최대 거리: [150] km                       │
│   시간 범위: [2] 시간                       │
│   [🔄 새로고침]                             │
├─────────────────────────────────────────────┤
│ 📊 추천 차량 (5건)                          │
│                                             │
│ ┌─────────────────────────────────────┐   │
│ │ 🚚 차량번호: 01가1234             │   │
│ │ 👤 운전자: 김운전 (010-1234-5678) │   │
│ │ 📍 현재: 서울 강남구              │   │
│ │ 📏 거리: 10.5 km | ⏱️ 20분        │   │
│ │ ⭐ 점수: 85.0 / 100               │   │
│ │ ✅ 대기 중, 거리 근접              │   │
│ └─────────────────────────────────────┘   │
│                                             │
│ [더 많은 차량...]                           │
├─────────────────────────────────────────────┤
│              [취소] [배차 완료]             │
└─────────────────────────────────────────────┘
```

**정보 항목:**
- **차량번호**: 차량 등록번호
- **운전자**: 이름 + 전화번호
- **현재 위치**: 주소 또는 "위치 정보 없음"
- **거리**: 상차지까지의 직선 거리 (km)
- **예상 도착**: 현재 위치에서 상차지까지 소요 시간 (분)
- **점수**: AI 추천 점수 (0-100)
  - 90-100: 매우 적합
  - 70-89: 적합
  - 50-69: 보통
  - 0-49: 부적합
- **이유**: 추천 사유 (예: "대기 중, 거리 근접")
- **경고**: 주의사항 (예: "배송 중", "정비 필요")

### Step 5: 차량 선택

**선택 방법:**
- 차량 카드 클릭
- 선택된 카드는 **파란색 테두리** 표시

**선택 기준 (예시):**
1. **점수 우선**: 가장 높은 점수의 차량
2. **거리 우선**: 가장 가까운 차량
3. **운전자 지정**: 특정 운전자 선호
4. **경고 확인**: 경고가 없는 차량

### Step 6: 배차 완료

**버튼 클릭:**
- "배차 완료" 버튼 클릭
- 확인 메시지 표시

**결과:**
```
✅ 배차가 완료되었습니다
차량: 01가1234
운전자: 김운전
```

**자동 업데이트:**
- 주문 상태: "배차대기" → "배차완료"
- 주문 목록 자동 갱신
- 배차 정보 DB에 저장

---

## 🔍 예상 결과

### ✅ 성공 케이스

#### 케이스 1: 차량이 있는 경우
```
✅ 추천 차량 5건

차량 1: 01가1234 (점수: 85.0)
- 운전자: 김운전
- 거리: 10.5 km
- 도착: 20분
- 상태: 대기 중

차량 2: 02나5678 (점수: 78.5)
- 운전자: 이기사
- 거리: 15.3 km
- 도착: 30분
- 상태: 대기 중

[차량 선택 후 배차 완료]
```

#### 케이스 2: 차량이 없는 경우
```
⚠️ 배차 가능한 차량이 없습니다

검색 조건:
- 최대 거리: 150 km
- 시간 범위: 2 시간

이유:
- 150km 반경 내에 대기 중인 차량이 없습니다
- 모든 차량이 배송 중이거나 정비 중입니다

해결 방법:
- 최대 거리를 늘려보세요 (예: 200 km, 300 km)
- 시간 범위를 늘려보세요 (예: 4 시간, 6 시간)
- 나중에 다시 시도해보세요
```

### ❌ 오류 케이스

#### 오류 1: 404 Not Found
```
Failed to load vehicle suggestions: Request failed with status code 404
```

**원인:** 브라우저 캐시에 오래된 JavaScript 파일이 로드됨

**해결:** [캐시 초기화](#캐시-초기화-필수) 참고

#### 오류 2: 401 Unauthorized
```
{"detail": "Not authenticated"}
```

**원인:** 로그인 세션 만료 또는 권한 부족

**해결:**
1. 다시 로그인
2. MANAGER, VEHICLE_MANAGER, ADMIN 권한 확인

#### 오류 3: 500 Internal Server Error
```
배차 중 오류가 발생했습니다
```

**원인:** 서버 내부 오류 (DB 연결, API 에러 등)

**해결:**
1. 백엔드 로그 확인:
   ```bash
   docker compose logs backend --tail=50
   ```
2. 개발팀 문의

---

## 🛠️ 문제 해결

### 문제 1: "AI 배차" 버튼이 보이지 않음

**체크리스트:**
- [ ] 주문 상태가 "배차대기"인가?
- [ ] 로그인 계정 권한이 MANAGER/VEHICLE_MANAGER/ADMIN인가?
- [ ] 브라우저 캐시를 초기화했는가?
- [ ] 페이지를 새로고침했는가? (F5)

**해결:**
1. 브라우저 캐시 초기화: http://139.150.11.99/clear-cache.html
2. 강력 새로고침: Ctrl+Shift+R (3-5회)
3. 시크릿 모드로 테스트

---

### 문제 2: 404 오류 발생

**증상:**
```
GET .../suggest-vehicles?... 404 (Not Found)
Failed to load vehicle suggestions
```

**원인:**
- 브라우저가 오래된 JavaScript 파일을 캐시함
- 이전 버전의 코드가 잘못된 API 엔드포인트를 호출

**진단:**
```
1. F12 → Console 탭
   - "404" 오류 메시지 확인

2. F12 → Network 탭
   - 실패한 요청 URL 확인
   - 로드된 JS 파일명 확인
     ❌ OrdersPage-2ebTppXZ.js (오래된 캐시)
     ✅ OrdersPage-XXXXXXXX.js (새 해시값)
```

**해결:**

**Step 1: 시크릿 모드로 확인**
```
Ctrl+Shift+N → http://139.150.11.99/orders
```
→ 시크릿 모드에서 정상 작동하면 캐시 문제 확정

**Step 2: 캐시 초기화 (선택)**

**옵션 A - 자동 (추천):**
```
http://139.150.11.99/clear-cache.html
→ "자동 캐시 초기화 및 재시작" 버튼 클릭
```

**옵션 B - 수동:**
```
1. Ctrl+Shift+Delete
2. "캐시된 이미지 및 파일" 선택
3. "데이터 삭제" 클릭
4. Ctrl+Shift+R 강력 새로고침 (3-5회)
```

**옵션 C - 개발자 도구:**
```
1. F12
2. Network 탭
3. "Disable cache" 체크
4. Ctrl+R 새로고침
```

**Step 3: 브라우저 재시작**
```
1. 브라우저 완전 종료
2. 다시 시작
3. http://139.150.11.99/orders 접속
```

**Step 4: 다른 브라우저 테스트**
```
Firefox, Safari, Opera 등에서 테스트
```

---

### 문제 3: "배차 가능한 차량이 없습니다"

**원인:**
- 실제로 150km 반경 내에 대기 중인 차량이 없음
- 모든 차량이 배송 중이거나 비활성 상태

**해결:**

**Step 1: 검색 범위 조정**
```
모달 상단에서 조정:
- 최대 거리: 150 km → 200 km, 300 km
- 시간 범위: 2 시간 → 4 시간, 6 시간
→ "새로고침" 버튼 클릭
```

**Step 2: 차량 상태 확인**
```
1. 차량 관리 페이지 접속
2. 차량 목록에서 상태 확인:
   - "대기 중" 차량이 있는가?
   - "배송 중" 또는 "정비 중" 상태인가?
3. 필요 시 차량 상태 변경
```

**Step 3: 테스트 차량 생성**
```
차량 관리 → 새 차량 추가:
- 차량번호: TEST-001
- 상태: 대기 중
- 위치: 서울 강남구 (주문 근처)
- 운전자: 김운전
```

---

### 문제 4: 모달이 열리지만 로딩만 계속됨

**증상:**
- 모달은 열림
- 로딩 스피너만 계속 돌아감
- 차량 목록이 표시되지 않음

**원인:**
- 백엔드 API 응답 지연 또는 오류
- 네트워크 연결 문제

**진단:**
```
F12 → Network 탭:
- suggest-vehicles 요청 상태 확인
- Pending (대기 중) → 서버 응답 지연
- 500 (서버 오류) → 백엔드 에러
- Failed (실패) → 네트워크 문제
```

**해결:**

**Step 1: 백엔드 로그 확인**
```bash
docker compose logs backend --tail=100 | grep -i error
```

**Step 2: 네트워크 연결 확인**
```bash
curl http://139.150.11.99/api/v1/health
```

**Step 3: 백엔드 재시작**
```bash
docker compose restart backend
sleep 10
docker compose ps
```

---

### 문제 5: 배차 완료 후 상태가 변경되지 않음

**증상:**
- "배차 완료" 버튼 클릭
- 성공 메시지 표시
- 하지만 주문 상태가 여전히 "배차대기"

**원인:**
- DB 업데이트 실패
- 프론트엔드 갱신 문제

**해결:**

**Step 1: 페이지 새로고침**
```
F5 또는 Ctrl+R
```

**Step 2: 백엔드 로그 확인**
```bash
docker compose logs backend --tail=50 | grep -i dispatch
```

**Step 3: DB 직접 확인**
```bash
docker compose exec db psql -U postgres -d coldchain -c "
SELECT id, order_number, status, vehicle_id 
FROM orders 
WHERE order_number = 'TEST-001';
"
```

---

## 📊 테스트 시나리오

### 시나리오 1: 기본 흐름 테스트

**목표:** AI 배차 기능의 전체 흐름 검증

**단계:**
```
1. ✅ http://139.150.11.99/orders 접속
2. ✅ manager1@test.com / test1234 로그인
3. ✅ "+" 버튼으로 새 주문 생성
   - 주문번호: TEST-BASIC-001
   - 상차지: 서울 강남구 테헤란로 123
   - 하차지: 부산 해운대구 해운대로 456
   - 온도: -18°C ~ -15°C
   - 팔레트: 10개
4. ✅ 저장 후 주문 목록에서 찾기
5. ✅ "AI 배차" 버튼 클릭
6. ✅ 모달 열림 확인
7. ✅ 추천 차량 목록 표시 확인
8. ✅ 최상위 차량 선택 (파란 테두리)
9. ✅ "배차 완료" 버튼 클릭
10. ✅ 성공 메시지 확인
11. ✅ 주문 상태 "배차완료"로 변경 확인
```

**예상 소요 시간:** 3-5분

---

### 시나리오 2: 검색 범위 조정 테스트

**목표:** 검색 조건 변경 시 결과 변화 검증

**단계:**
```
1. ✅ 배차대기 주문 선택
2. ✅ "AI 배차" 버튼 클릭
3. ✅ 기본 검색 결과 확인 (150km, 2시간)
4. ✅ 최대 거리를 50km로 변경
5. ✅ "새로고침" 클릭
6. ✅ 결과가 줄어드는지 확인
7. ✅ 최대 거리를 300km로 변경
8. ✅ "새로고침" 클릭
9. ✅ 결과가 늘어나는지 확인
10. ✅ 시간 범위를 6시간으로 변경
11. ✅ "새로고침" 클릭
12. ✅ 결과 변화 확인
```

**예상 소요 시간:** 5-7분

---

### 시나리오 3: 캐시 초기화 테스트

**목표:** 캐시 문제 해결 방법 검증

**단계:**
```
1. ✅ 일반 브라우저에서 404 오류 재현
2. ✅ http://139.150.11.99/clear-cache.html 접속
3. ✅ "자동 캐시 초기화 및 재시작" 버튼 클릭
4. ✅ 5초 후 자동 리다이렉트 확인
5. ✅ 로그인
6. ✅ "AI 배차" 기능 정상 작동 확인
7. ✅ F12 → Network 탭에서 새 파일 로드 확인
```

**예상 소요 시간:** 2-3분

---

### 시나리오 4: 시크릿 모드 테스트

**목표:** 캐시 없이 정상 작동 검증

**단계:**
```
1. ✅ Ctrl+Shift+N (시크릿 모드)
2. ✅ http://139.150.11.99/orders 접속
3. ✅ manager1@test.com / test1234 로그인
4. ✅ 배차대기 주문 선택
5. ✅ "AI 배차" 버튼 클릭
6. ✅ 모달 열림 및 차량 목록 표시 확인
7. ✅ 차량 선택 및 배차 완료
8. ✅ 시크릿 모드 종료
9. ✅ 일반 브라우저에서 캐시 초기화 수행
10. ✅ 일반 브라우저에서도 정상 작동 확인
```

**예상 소요 시간:** 4-6분

---

## 📞 지원 및 문의

### 기술 지원

**문제 발생 시 수집할 정보:**
1. **오류 메시지** (F12 → Console 탭)
2. **네트워크 요청** (F12 → Network 탭)
3. **백엔드 로그** (관리자에게 요청)
4. **재현 단계** (어떤 순서로 문제 발생?)

**보고 템플릿:**
```
[문제 제목]
AI 배차 버튼 클릭 시 404 오류 발생

[발생 환경]
- 브라우저: Chrome 120
- OS: Windows 11
- 로그인 계정: manager1@test.com

[재현 단계]
1. 주문 페이지 접속
2. 배차대기 주문 선택
3. AI 배차 버튼 클릭
4. 404 오류 발생

[오류 메시지]
Failed to load vehicle suggestions: Request failed with status code 404

[시도한 해결 방법]
- 캐시 초기화: 시도함
- 시크릿 모드: 시크릿 모드에서는 정상 작동함

[스크린샷]
(F12 Console 탭 캡처 첨부)
```

---

## 📚 참고 자료

### API 문서
```
http://139.150.11.99:8000/docs
→ "Semi-Auto Dispatch" 섹션 참고
```

### 관련 파일
- **Backend**:
  - `/backend/app/api/semi_auto_dispatch.py`
  - `/backend/app/services/semi_auto_dispatch_service.py`
- **Frontend**:
  - `/frontend/src/api/semi-auto-dispatch.ts`
  - `/frontend/src/components/orders/SemiAutoDispatchModal.tsx`
  - `/frontend/src/pages/OrdersPage.tsx`

### 커밋 히스토리
- `f1a6d01`: AI 배차 시스템 초기 구현
- `4b96ed5`: POST → GET 메소드 변경
- `07bfafa`: 캐시 초기화 페이지 추가

---

## ✅ 체크리스트

### 배포 완료 체크리스트
- [x] Backend API 엔드포인트 등록 확인
- [x] Frontend 빌드 및 배포
- [x] 캐시 초기화 페이지 생성
- [x] 헬스체크 통과
- [x] API 문서 업데이트
- [ ] 사용자 공지 발송
- [ ] 테스트 시나리오 실행
- [ ] 피드백 수집

### 사용자 온보딩 체크리스트
- [ ] 캐시 초기화 페이지 URL 공유
- [ ] 사용 가이드 문서 공유
- [ ] 테스트 계정 제공 (manager1@test.com)
- [ ] 시연 영상 또는 스크린샷 제공
- [ ] 문의 채널 안내

---

**마지막 업데이트:** 2026-03-02
**문서 버전:** 1.0
**작성자:** AI Development Team
