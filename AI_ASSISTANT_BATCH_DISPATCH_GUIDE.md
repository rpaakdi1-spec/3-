# AI 어시스턴트 배차 일괄 등록 가이드

## 📋 개요

AI 어시스턴트에서 배차 텍스트를 붙여넣으면 자동으로 파싱하고 팔레트 수를 계산하여 명확한 형식으로 표시합니다.

## 🎯 기능

### 입력 형식
```
**3/5목우촌 오후배차**
13:00 / 식육11톤(냉동)
13:30 / 육가공11톤
14:30 / 육가공5톤
15:00 / 식육11톤
15:30 / 육가공5톤
```

### 출력 형식
```
✅ 5건의 주문이 접수되었습니다.

3/5일 목우촌
13:00 식육 16p
13:30 육가공 16p
14:30 육가공 10p
15:00 식육 16p
15:30 육가공 10p
```

## 🔧 구현 내역

### 1. 배차 형식 감지 및 파싱 (`backend/app/services/ai_chat_service.py`)

#### `_extract_batch_dispatch_orders()` 함수
- **헤더 파싱**: `**3/5목우촌 오후배차**` 형식에서 날짜와 고객명 추출
- **날짜 처리**: 현재 연도 기준으로 날짜 객체 생성
- **주문 라인 파싱**: 정규식으로 시간, 품목, 톤수, 온도 추출
  ```python
  line_pattern = r'(\d{1,2}:\d{2})\s*/\s*([가-힣a-zA-Z0-9]+?)\s*(\d+(?:\.\d+)?)\s*톤\s*(?:\(([^)]+)\))?'
  ```

#### 톤수 → 팔레트 변환 규칙
| 톤수 | 팔레트 |
|------|--------|
| ≥18톤 | 18p |
| ≥11톤 | 16p |
| ≥5톤 | 10p |
| <5톤 | 톤당 2p |

#### 온도대 판정
- `(냉동)` 표시 있으면 → 냉동
- 표시 없으면 → 냉장 (기본값)

#### `_format_batch_dispatch_confirmation()` 함수
- 파싱된 주문 정보를 사용자 친화적 형식으로 포맷
- 날짜, 고객명, 시간, 품목, 팔레트 수 표시

### 2. 여러 주문 생성 응답 개선 (`backend/app/api/ai_chat.py`)

주문 생성 완료 시 메시지를 다음과 같이 개선:

**변경 전:**
```
✅ 5개 주문이 등록되었습니다!

주문번호:
• ORD-20260305-001
• ORD-20260305-002
• ORD-20260305-003
• ORD-20260305-004
• ORD-20260305-005
```

**변경 후:**
```
✅ 5건의 주문이 접수되었습니다.

3/5일 목우촌
13:00 식육 16p
13:30 육가공 16p
14:30 육가공 10p
15:00 식육 16p
15:30 육가공 10p
```

### 3. 처리 흐름

1. 사용자가 AI 어시스턴트에 배차 텍스트 입력
2. `_process_with_simulation()` 메서드가 배차 형식 감지
3. `_extract_batch_dispatch_orders()` 함수가 파싱 수행
4. 파싱된 주문 정보로 확인 메시지 생성
5. 사용자가 "네" 또는 "확인" 입력
6. 주문 생성 후 요약 메시지 표시

## 📝 사용 방법

### AI 어시스턴트 탭 사용
1. **주문관리** 페이지로 이동 (http://139.150.11.99/orders)
2. **AI 어시스턴트** 탭 클릭
3. 배차 텍스트 붙여넣기:
   ```
   **3/5목우촌 오후배차**
   13:00 / 식육11톤(냉동)
   13:30 / 육가공11톤
   14:30 / 육가공5톤
   15:00 / 식육11톤
   15:30 / 육가공5톤
   ```
4. AI가 파싱된 내용 확인 요청
5. "네" 또는 "확인" 입력
6. 주문 접수 완료 메시지 확인

### 배차 일괄등록 모달 사용 (기존 기능)
- 녹색 **"배차 일괄등록"** 버튼 클릭
- 별도 모달에서 배차 텍스트 입력
- 상/하차지 주소 입력 필요

## 🔍 지원 형식 예시

### 표준 형식
```
**3/5목우촌 오후배차**
13:00 / 식육11톤(냉동)
13:30 / 육가공11톤
```

### 요일 포함
```
3/5(월)목우촌 오후배차
13:00 / 식육11톤(냉동)
```

### 다양한 톤수
```
**2/23(월)배차**
08:00 / 상품A 18톤
10:00 / 상품B 11톤
12:00 / 상품C 5톤
14:00 / 상품D 3톤
```

결과:
```
✅ 4건의 주문이 접수되었습니다.

2/23일
08:00 상품A 18p
10:00 상품B 16p
12:00 상품C 10p
14:00 상품D 6p
```

## 🚀 배포 방법

```bash
cd /root/uvis
git pull origin main
docker compose build backend
docker compose up -d backend
sleep 10
curl http://139.150.11.99/api/v1/health
```

## 📊 커밋 정보

- **커밋 해시**: `783ea57`
- **커밋 메시지**: "feat: enhance AI assistant to format batch dispatch orders with date and pallet summary"
- **변경 파일**:
  - `backend/app/api/ai_chat.py` (+40 lines)
  - `backend/app/services/ai_chat_service.py` (+122 lines)

## ✅ 테스트 체크리스트

- [ ] AI 어시스턴트 탭에서 배차 텍스트 입력
- [ ] 파싱 결과 확인 (날짜, 고객명, 시간, 품목, 팔레트)
- [ ] "네" 입력 후 주문 생성 확인
- [ ] 생성된 주문 요약 메시지 확인
- [ ] 주문 목록에서 실제 주문 생성 확인

## 🔗 관련 문서

- [배차 일괄등록 기능](./BATCH_DISPATCH_REGISTRATION.md)
- [AI 어시스턴트 가이드](./AI_ASSISTANT_GUIDE.md) (예정)
- [주문 관리 API](./API_ORDERS.md) (예정)

---

**마지막 업데이트**: 2026-03-02
**작성자**: AI Assistant
**버전**: 1.0.0
