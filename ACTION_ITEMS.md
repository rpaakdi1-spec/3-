# 🎯 이제 할 일 - 우선순위별 액션 아이템

## 📅 생성일: 2026-01-29
## 📌 상태: 준비 완료 → 배포 단계

---

## 🔴 높은 우선순위 (즉시 실행)

### 1️⃣ 프로덕션 서버 배포 [15-20분 소요]

#### 목표
최신 코드(온도 자동입력 기능 포함)를 프로덕션 환경에 배포

#### 전제 조건
- ✅ PR #1 병합 완료
- ✅ main 브랜치 최신 커밋: `7780df5`
- ✅ 배포 가이드 문서: `DEPLOYMENT_INSTRUCTIONS.md`

#### 실행 단계

**Step 1: 서버 접속**
```bash
# 로컬 터미널에서 실행
ssh root@139.150.11.99
```

**Step 2: 프로젝트 디렉토리로 이동**
```bash
cd /root/uvis
```

**Step 3: 현재 브랜치 및 상태 확인**
```bash
# 현재 브랜치 확인
git branch

# 현재 커밋 확인
git log --oneline -3

# 변경사항 확인
git status
```

**Step 4: 최신 코드 가져오기**
```bash
# main 브랜치로 전환
git checkout main

# 최신 코드 가져오기
git fetch origin main

# 코드 병합
git pull origin main

# 최신 커밋 확인 (7780df5 또는 그 이후)
git log --oneline -1
```

**Step 5: 서비스 중지**
```bash
docker-compose -f docker-compose.prod.yml down
```

**Step 6: 이미지 재빌드 (캐시 없이)**
```bash
# 프론트엔드 재빌드
docker-compose -f docker-compose.prod.yml build --no-cache frontend

# 백엔드 재빌드
docker-compose -f docker-compose.prod.yml build backend
```

**Step 7: 서비스 시작**
```bash
docker-compose -f docker-compose.prod.yml up -d
```

**Step 8: 서비스 상태 확인**
```bash
# 컨테이너 상태 확인
docker-compose -f docker-compose.prod.yml ps

# 프론트엔드 로그 확인 (Ctrl+C로 종료)
docker-compose -f docker-compose.prod.yml logs -f frontend
```

**Step 9: 서비스 접근 테스트**
```bash
# 로컬 터미널 (새 탭)에서 실행
curl http://139.150.11.99
curl http://139.150.11.99:8000/health
```

#### 성공 기준
- ✅ 모든 컨테이너 `Up` 또는 `healthy` 상태
- ✅ Frontend 접근 가능 (http://139.150.11.99)
- ✅ Backend 접근 가능 (http://139.150.11.99:8000)

#### 문제 발생 시
```bash
# 로그 확인
docker-compose -f docker-compose.prod.yml logs

# 특정 서비스 재시작
docker-compose -f docker-compose.prod.yml restart frontend
docker-compose -f docker-compose.prod.yml restart backend

# 전체 재시작
docker-compose -f docker-compose.prod.yml restart
```

---

### 2️⃣ 온도 자동입력 기능 테스트 [10분 소요]

#### 목표
배포된 환경에서 온도 자동입력 기능이 정상 작동하는지 검증

#### 테스트 시나리오

**시나리오 1: 냉동 온도 자동입력**
1. URL 접속: http://139.150.11.99/orders
2. 로그인 (admin / 기존 비밀번호)
3. "+ 신규 등록" 버튼 클릭
4. 온도대 드롭다운 → "냉동" 선택
5. **예상 결과**:
   - 최저 온도: `-30` 자동 입력
   - 최고 온도: `-18` 자동 입력
   - 안내 메시지 표시

**시나리오 2: 냉장 온도 자동입력**
1. 온도대 드롭다운 → "냉장" 선택
2. **예상 결과**:
   - 최저 온도: `0` 자동 입력
   - 최고 온도: `6` 자동 입력

**시나리오 3: 상온 온도 자동입력**
1. 온도대 드롭다운 → "상온" 선택
2. **예상 결과**:
   - 최저 온도: `-30` 자동 입력
   - 최고 온도: `60` 자동 입력

**시나리오 4: 수동 수정 가능성 확인**
1. 냉동 선택 → 자동 입력 확인
2. 최저 온도를 `-25`로 변경
3. 최고 온도를 `-20`로 변경
4. **예상 결과**: 값 변경 가능

**시나리오 5: 주문 등록 완료**
1. 필수 필드 입력:
   - 거래처: 테스트 거래처
   - 출발지: 서울시 강남구
   - 도착지: 경기도 성남시
   - 팔레트 수량: 5
   - 온도대: 냉동 (자동 입력된 온도 유지)
2. "등록" 버튼 클릭
3. **예상 결과**: 
   - "주문이 등록되었습니다" 메시지
   - 주문 목록에 추가됨

#### 테스트 체크리스트
```
☐ 냉동 선택 → -30 ~ -18 자동 입력
☐ 냉장 선택 → 0 ~ 6 자동 입력
☐ 상온 선택 → -30 ~ 60 자동 입력
☐ 자동 입력된 값 수동 수정 가능
☐ 안내 메시지 표시
☐ 주문 등록 성공
☐ 등록된 주문 조회 가능
```

#### 문제 발생 시
**증상**: 자동 입력이 작동하지 않음
**해결**:
1. 브라우저 캐시 삭제 (Ctrl+Shift+R)
2. 브라우저 콘솔(F12) 확인
3. 에러 메시지 복사
4. 프론트엔드 로그 확인:
   ```bash
   docker-compose -f docker-compose.prod.yml logs frontend | tail -100
   ```

**증상**: 422 Unprocessable Entity 오류
**해결**:
1. 브라우저 완전히 닫기
2. 캐시 삭제
3. 새로 시작

---

### 3️⃣ 서비스 상태 종합 확인 [5분 소요]

#### 목표
모든 서비스가 정상 작동하는지 확인

#### 체크리스트

**컨테이너 상태**
```bash
docker-compose -f docker-compose.prod.yml ps
```
```
☐ uvis-frontend: Up (0.0.0.0:3000->3000/tcp)
☐ uvis-backend: Up, healthy (0.0.0.0:8000->8000/tcp)
☐ uvis-db: Up, healthy (0.0.0.0:5432->5432/tcp)
☐ uvis-redis: Up, healthy (0.0.0.0:6379->6379/tcp)
☐ uvis-nginx: Up (0.0.0.0:80->80/tcp)
```

**서비스 접근성**
```bash
# Frontend
curl -I http://139.150.11.99
# 예상: HTTP/1.1 200 OK

# Backend
curl http://139.150.11.99:8000/health
# 예상: {"status":"ok"}

# API Docs
curl -I http://139.150.11.99:8000/docs
# 예상: HTTP/1.1 200 OK
```

**데이터베이스 연결**
```bash
docker-compose -f docker-compose.prod.yml exec backend python -c "
from app.core.database import engine
print('Database connection:', engine.url)
"
```

**Redis 연결**
```bash
docker-compose -f docker-compose.prod.yml exec redis redis-cli ping
# 예상: PONG
```

#### 리소스 사용량
```bash
# CPU/메모리 사용량
docker stats --no-stream

# 디스크 사용량
df -h

# 메모리 사용량
free -h
```

---

## 🟡 중간 우선순위 (24시간 내)

### 4️⃣ 주문 캘린더 기능 테스트 [15분 소요]

#### 목표
주문 캘린더의 고급 기능 검증

#### 테스트 시나리오

**드래그앤드롭 테스트**
1. URL: http://139.150.11.99/calendar
2. 기존 주문을 다른 날짜로 드래그
3. **예상**: 주문 날짜 변경, 확인 메시지

**빠른 주문 등록**
1. 캘린더에서 날짜 클릭
2. 빠른 등록 모달 표시
3. 최소 정보 입력 후 등록
4. **예상**: 주문 생성, 캘린더에 표시

**색상 범례 확인**
```
☐ 대기중: 파란색
☐ 진행중: 노란색
☐ 완료: 초록색
☐ 취소: 회색
```

**뷰 전환**
```
☐ 월간 보기
☐ 주간 보기
☐ 일간 보기
```

---

### 5️⃣ 사용자/QA 팀 테스트 요청 [30분 소요]

#### 목표
실제 사용자 관점에서 기능 검증 및 피드백 수집

#### 테스트 가이드 공유

**이메일/메시지 템플릿:**
```
제목: [테스트 요청] 주문 관리 온도 자동입력 기능

안녕하세요,

주문 관리 시스템에 새로운 기능이 추가되었습니다.
아래 내용을 확인하고 테스트 부탁드립니다.

【 배포 정보 】
- 배포 일시: 2026-01-29
- 배포 범위: 주문 등록 온도 자동입력 기능
- 접속 URL: http://139.150.11.99/orders

【 새로운 기능 】
주문 등록 시 온도대를 선택하면 권장 온도 범위가 자동으로 입력됩니다:
- 냉동: -30°C ~ -18°C
- 냉장: 0°C ~ 6°C
- 상온: -30°C ~ 60°C

【 테스트 시나리오 】
1. 주문 관리 페이지 접속
2. "+ 신규 등록" 버튼 클릭
3. 온도대 드롭다운에서 각 옵션 선택
4. 온도 범위 자동 입력 확인
5. 필요시 온도 수동 조정
6. 주문 등록 완료

【 피드백 요청 사항 】
- 기능 작동 여부
- 사용 편의성
- 개선 필요 사항
- 발견된 버그

【 응답 기한 】
2026-01-30 18:00까지

감사합니다.
```

#### 피드백 수집 양식
```markdown
## 테스트 결과 보고서

### 기본 정보
- 테스터 이름:
- 테스트 일시:
- 브라우저: Chrome / Firefox / Safari / Edge
- 버전:

### 기능 테스트
#### 1. 온도 자동입력
- [ ] 냉동 선택 시 -30 ~ -18 자동 입력
- [ ] 냉장 선택 시 0 ~ 6 자동 입력
- [ ] 상온 선택 시 -30 ~ 60 자동 입력
- [ ] 수동 수정 가능

#### 2. 주문 등록
- [ ] 등록 성공
- [ ] 등록된 주문 조회 가능

### 발견된 문제
(있는 경우 상세히 기술)

### 개선 제안
(있는 경우 기술)

### 전체 만족도
- [ ] 매우 만족
- [ ] 만족
- [ ] 보통
- [ ] 불만족
- [ ] 매우 불만족

### 추가 의견
```

---

### 6️⃣ 에러 로그 모니터링 [지속적]

#### 목표
배포 후 발생하는 에러를 실시간으로 모니터링하고 대응

#### 모니터링 명령어

**실시간 로그 모니터링**
```bash
# 모든 서비스 로그
docker-compose -f docker-compose.prod.yml logs -f

# 에러만 필터링
docker-compose -f docker-compose.prod.yml logs -f | grep -i error

# 프론트엔드 에러
docker-compose -f docker-compose.prod.yml logs -f frontend | grep -i error

# 백엔드 에러
docker-compose -f docker-compose.prod.yml logs -f backend | grep -i error
```

**로그 파일로 저장**
```bash
# 최근 1시간 로그 저장
docker-compose -f docker-compose.prod.yml logs --since 1h > logs_$(date +%Y%m%d_%H%M%S).txt

# 에러 로그만 저장
docker-compose -f docker-compose.prod.yml logs | grep -i error > errors_$(date +%Y%m%d_%H%M%S).txt
```

#### 주요 확인 사항
```
☐ 500 Internal Server Error
☐ 422 Unprocessable Entity
☐ 404 Not Found
☐ Database connection errors
☐ Redis connection errors
☐ CORS errors
☐ Authentication errors
```

#### 알림 설정 (선택사항)
```bash
# 에러 발생 시 이메일 알림 (예시)
docker-compose -f docker-compose.prod.yml logs -f | \
  grep -i error | \
  while read line; do
    echo "$line" | mail -s "UVIS Error Alert" admin@example.com
  done
```

---

## 🟢 낮은 우선순위 (1주일 내)

### 7️⃣ UX 개선 검토 [2시간 소요]

#### 개선 사항 1: 드롭다운에 온도 범위 표시

**현재**
```
온도대: [ 냉동 ▼ ]
```

**개선안**
```
온도대: [ 냉동 (-30°C ~ -18°C) ▼ ]
        [ 냉장 (0°C ~ 6°C) ▼ ]
        [ 상온 (-30°C ~ 60°C) ▼ ]
```

**구현 방법**
```typescript
// frontend/src/components/orders/OrderModal.tsx
<select 
  value={formData.cargo_type}
  onChange={(e) => setFormData({...formData, cargo_type: e.target.value})}
>
  <option value="">선택하세요</option>
  <option value="냉동">냉동 (-30°C ~ -18°C)</option>
  <option value="냉장">냉장 (0°C ~ 6°C)</option>
  <option value="상온">상온 (-30°C ~ 60°C)</option>
</select>
```

#### 개선 사항 2: 입력 필드 툴팁

**구현 예시**
```typescript
<div className="relative group">
  <label>최저 온도</label>
  <input type="number" value={formData.min_temp} />
  <div className="tooltip hidden group-hover:block">
    선택한 온도대의 권장 온도입니다. 필요시 수정 가능합니다.
  </div>
</div>
```

#### 개선 사항 3: 유효성 검증 강화

**검증 규칙**
```typescript
// 최저 온도가 최고 온도보다 높으면 에러
if (formData.min_temp > formData.max_temp) {
  setError('최저 온도는 최고 온도보다 낮아야 합니다.');
  return;
}

// 온도대별 권장 범위 벗어나면 경고
if (formData.cargo_type === '냉동' && 
    (formData.min_temp < -40 || formData.max_temp > 0)) {
  setWarning('냉동 화물의 권장 온도 범위는 -30°C ~ -18°C입니다.');
}
```

---

### 8️⃣ 성능 모니터링 설정 [1시간 소요]

#### 목표
시스템 성능 지표를 수집하고 대시보드에서 확인

#### Prometheus 설정 (이미 구성됨)
```bash
# Prometheus 상태 확인
curl http://139.150.11.99:9090/metrics

# 주요 메트릭 확인
curl http://139.150.11.99:9090/api/v1/query?query=up
```

#### 주요 모니터링 지표

**응답 시간**
- API 평균 응답 시간: < 200ms
- 주문 등록 API: < 500ms
- 주문 조회 API: < 100ms

**에러율**
- 5xx 에러율: < 0.1%
- 4xx 에러율: < 1%

**리소스 사용량**
- CPU 사용률: < 70%
- 메모리 사용률: < 80%
- 디스크 사용률: < 85%

**데이터베이스**
- 쿼리 평균 실행 시간: < 50ms
- 활성 커넥션 수: < 50

#### Grafana 대시보드 (선택사항)
```bash
# Grafana 접속
http://139.150.11.99:3001

# 로그인
username: admin
password: admin
```

**대시보드 생성**
1. 새 대시보드 추가
2. Prometheus 데이터소스 선택
3. 패널 추가:
   - API 응답 시간 그래프
   - 에러율 차트
   - 리소스 사용량 게이지

---

## 📊 진행 상황 추적

### 완료 체크리스트

**즉시 (오늘)**
```
☐ 프로덕션 배포
☐ 온도 자동입력 테스트
☐ 서비스 상태 확인
```

**24시간 내**
```
☐ 주문 캘린더 테스트
☐ 사용자 테스트 요청
☐ 에러 로그 확인
```

**1주일 내**
```
☐ UX 개선 계획 수립
☐ 성능 모니터링 대시보드
☐ 피드백 분석 및 개선
```

---

## 📞 문제 발생 시 대응

### 긴급 연락처
- 개발팀: [연락처]
- 서버 관리자: [연락처]
- 프로젝트 매니저: [연락처]

### 에스컬레이션 기준

**Level 1 (낮음)**
- UI 버그
- 사소한 표시 오류
→ 이슈 티켓 생성, 다음 업데이트에 수정

**Level 2 (중간)**
- 특정 기능 작동 안 함
- 성능 저하
→ 즉시 수정 시작, 1-2일 내 배포

**Level 3 (높음)**
- 주요 기능 불능
- 데이터 손실 위험
→ 즉시 수정, 당일 핫픽스 배포

**Level 4 (긴급)**
- 서비스 전체 다운
- 보안 취약점 발견
→ 즉시 롤백 또는 긴급 패치

---

## ✅ 완료 기준

모든 항목이 완료되면:

1. ✅ 프로덕션 배포 완료
2. ✅ 모든 기능 테스트 통과
3. ✅ 사용자 피드백 긍정적
4. ✅ 에러 없이 안정적 운영
5. ✅ 성능 지표 목표 달성

**다음 단계**: 추가 기능 개발 또는 최적화 작업

---

**문서 버전**: 1.0  
**최종 업데이트**: 2026-01-29  
**작성자**: GenSpark AI Developer
