# 🚀 Phase 3 배포 가이드

## 📋 Phase 3 개요

**Phase 3: 프로덕션 통합 & A/B 테스트**

ML 배차 시스템을 실제 프로덕션 환경에 안전하게 배포하고, A/B 테스트를 통해 점진적으로 롤아웃합니다.

---

## ✅ Phase 3 완료 항목

### 1. 백엔드 구현 ✅
- ✅ A/B 테스트 서비스 (`backend/app/services/ab_test_service.py`)
- ✅ ML Dispatch API 엔드포인트 (`backend/app/api/ml_dispatch.py`)
  - `/api/ml-dispatch/ab-test/assignment` - 사용자 그룹 조회
  - `/api/ml-dispatch/ab-test/rollout` - 롤아웃 비율 조정
  - `/api/ml-dispatch/ab-test/stats` - 실험 통계
  - `/api/ml-dispatch/ab-test/metrics` - 성과 비교
  - `/api/ml-dispatch/ab-test/history` - 롤아웃 이력
  - `/api/ml-dispatch/ab-test/force-assign` - 강제 그룹 할당
- ✅ 자동 롤백 시스템 (`backend/app/services/auto_rollback_service.py`)

### 2. 프론트엔드 구현 ✅
- ✅ ML 추천 패널 (`frontend/src/components/ml/MLRecommendationPanel.tsx`)
- ✅ A/B 테스트 모니터 (`frontend/src/components/ml/ABTestMonitor.tsx`)
- ✅ ML Dispatch API 클라이언트 (`frontend/src/services/api.ts`)

### 3. 배포 자동화 ✅
- ✅ 점진적 롤아웃 스크립트 (`scripts/gradual_rollout.sh`)

---

## 🏗️ 아키텍처

```
┌─────────────────────────────────────────────────────────────┐
│                    Frontend (React)                          │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  DispatchOptimization.tsx                            │   │
│  │    ├── 기존 배차 (Control)                           │   │
│  │    └── ML 추천 패널 (Treatment)                      │   │
│  │         ├── Top 3 차량 추천                          │   │
│  │         ├── 상세 점수 표시                           │   │
│  │         └── 1-Click 배차                             │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                  A/B Test Controller (Redis)                 │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Traffic Split                                       │   │
│  │    ├── Control Group (기존 시스템)                   │   │
│  │    ├── Treatment Group (ML 시스템)                   │   │
│  │    └── Gradual Rollout (10% → 30% → 50% → 100%)    │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────────────┐
│                   Auto Rollback System                       │
│  ┌──────────────────────────────────────────────────────┐   │
│  │  Real-time Monitoring                                │   │
│  │    ├── Success Rate ≥ 90%                           │   │
│  │    ├── ML Score ≥ 0.70                              │   │
│  │    ├── Error Rate < 5%                              │   │
│  │    └── Response Time < 5s                           │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📦 배포 파일 목록

### 백엔드 파일 (총 3개)
```
backend/app/services/ab_test_service.py         (12.4KB)
backend/app/services/auto_rollback_service.py   (9.7KB)
backend/app/api/ml_dispatch.py                  (25.8KB) - A/B 테스트 엔드포인트 추가
```

### 프론트엔드 파일 (총 3개)
```
frontend/src/components/ml/MLRecommendationPanel.tsx  (9.7KB)
frontend/src/components/ml/ABTestMonitor.tsx          (12.5KB)
frontend/src/services/api.ts                          (updated with mlDispatchAPI)
```

### 스크립트 파일 (총 1개)
```
scripts/gradual_rollout.sh  (6.8KB)
```

**전체 추가 용량: 약 76.9KB**

---

## 🚀 배포 단계

### 1️⃣ 사전 준비 (5분)

#### Redis 설정 확인
```bash
# Redis가 실행 중인지 확인
docker ps | grep redis

# Redis 연결 테스트
docker exec -it uvis-redis redis-cli ping
# 응답: PONG
```

#### 환경 변수 설정
```bash
# docker-compose.prod.yml 또는 .env 파일에 추가
REDIS_HOST=redis
REDIS_PORT=6379
```

---

### 2️⃣ 서버 배포 (15분)

#### Step 1: 코드 배포
```bash
# 서버 접속
ssh root@139.150.11.99

# 프로젝트 디렉토리로 이동
cd /root/uvis

# Git pull
git pull origin main

# 권한 설정
chmod +x scripts/gradual_rollout.sh
```

#### Step 2: Redis 서비스 시작 (필요 시)
```bash
# docker-compose.prod.yml에 Redis 추가되어 있는지 확인
cat docker-compose.prod.yml | grep redis

# Redis가 없으면 추가
docker-compose -f docker-compose.prod.yml up -d redis
```

#### Step 3: 백엔드 재빌드 및 재시작
```bash
# 백엔드 재빌드
docker-compose -f docker-compose.prod.yml up -d --build backend

# 로그 확인
docker logs uvis-backend --tail 50 -f
```

#### Step 4: 프론트엔드 재빌드 및 재시작
```bash
# 프론트엔드 재빌드
docker-compose -f docker-compose.prod.yml up -d --build frontend

# 로그 확인
docker logs uvis-frontend --tail 50 -f
```

---

### 3️⃣ 동작 확인 (10분)

#### 백엔드 헬스 체크
```bash
# ML Dispatch API 확인
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/stats

# 응답 예시:
# {
#   "total_users": 0,
#   "control_count": 0,
#   "treatment_count": 0,
#   "actual_treatment_percentage": 0.0,
#   "target_rollout_percentage": 10
# }
```

#### 프론트엔드 확인
```bash
# 브라우저에서 접속
http://139.150.11.99

# 로그인 후 배차 최적화 페이지 접속
# ML 추천 패널이 표시되는지 확인
```

#### API 문서 확인
```bash
# Swagger UI 접속
http://139.150.11.99:8000/docs

# ML Dispatch 섹션 확인
# - POST /api/ml-dispatch/optimize
# - GET /api/ml-dispatch/ab-test/assignment
# - POST /api/ml-dispatch/ab-test/rollout
# - GET /api/ml-dispatch/ab-test/stats
# - GET /api/ml-dispatch/ab-test/metrics
```

---

### 4️⃣ 점진적 롤아웃 (4주 권장)

#### Week 1: 파일럿 (10% 롤아웃)

```bash
# 롤아웃 실행
./scripts/gradual_rollout.sh pilot

# 또는 수동으로:
curl -X POST "http://139.150.11.99:8000/api/ml-dispatch/ab-test/rollout?percentage=10" \
  -H "Authorization: Bearer YOUR_TOKEN"

# 1시간 대기 후 메트릭 확인
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/metrics
```

**파일럿 기간 체크리스트:**
- [ ] ML 추천 정확도 확인
- [ ] 응답 시간 모니터링
- [ ] 에러 로그 확인
- [ ] 사용자 피드백 수집

**성공 기준:**
- ✅ ML 성공률 ≥ 90%
- ✅ 평균 ML 점수 ≥ 0.70
- ✅ 에러율 < 5%
- ✅ 응답 시간 < 2초

---

#### Week 2: 확대 (30% 롤아웃)

```bash
# 파일럿 성공 시 30%로 확대
./scripts/gradual_rollout.sh expand

# 2시간 대기 후 메트릭 확인
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/metrics
```

**확대 기간 체크리스트:**
- [ ] Control vs Treatment 성과 비교
- [ ] 시스템 안정성 확인
- [ ] 추가 사용자 피드백 수집

---

#### Week 3: 절반 (50% 롤아웃)

```bash
# 확대 성공 시 50%로 증가
./scripts/gradual_rollout.sh half

# 4시간 대기 후 메트릭 확인
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/metrics
```

---

#### Week 4: 전면 (100% 롤아웃)

```bash
# 절반 성공 시 전면 배포
./scripts/gradual_rollout.sh full

# 메트릭 모니터링 지속
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/metrics
```

**전면 배포 후:**
- [ ] ML 배차를 기본값으로 설정
- [ ] 기존 배차 시스템을 백업으로 유지
- [ ] 지속적인 성능 모니터링

---

### 5️⃣ 자동 롤백 시스템

#### 롤백 트리거 조건
- ❌ 에러율 > 5%
- ❌ 성공률 < 90%
- ❌ ML 점수 < 0.60
- ❌ 응답 시간 > 5초
- ❌ 연속 3회 실패

#### 수동 롤백
```bash
# 즉시 롤백 (0%로 복구)
./scripts/gradual_rollout.sh rollback

# 또는 API로:
curl -X POST "http://139.150.11.99:8000/api/ml-dispatch/ab-test/rollout?percentage=0" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

#### 롤백 후 조치
1. 로그 분석 (`docker logs uvis-backend --tail 200`)
2. 에러 원인 파악
3. 코드 수정 및 재배포
4. 파일럿부터 다시 시작

---

## 📊 모니터링 대시보드

### 실시간 메트릭 확인
```bash
# A/B 테스트 통계
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/stats

# 성과 비교
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/metrics

# 롤아웃 이력
curl http://139.150.11.99:8000/api/ml-dispatch/ab-test/history?limit=20
```

### 프론트엔드 대시보드
- **URL**: `http://139.150.11.99/ab-test-monitor`
- **기능**:
  - Control vs Treatment 성과 비교
  - 실시간 메트릭 차트
  - 롤아웃 비율 조정 UI
  - 롤아웃 이력 타임라인

---

## 🎯 성공 기준 (KPI)

### Phase 3 목표
| 메트릭 | 목표 | 측정 방법 |
|--------|------|-----------|
| ML 성공률 | ≥ 90% | A/B Test Metrics API |
| ML 평균 점수 | ≥ 0.70 | A/B Test Metrics API |
| 에러율 | < 5% | Auto Rollback System |
| 응답 시간 | < 2초 | A/B Test Metrics API |
| 사용자 만족도 | ≥ 4.0/5.0 | 사용자 설문 |

### 비즈니스 효과
- **거리 절감**: 30% (120km → 84km)
- **회전수 편차**: 50% 감소 (4 → 2회)
- **정시 배송률**: 95% (87% → 95%)
- **연간 절감액**: 5.5억원

---

## 🔧 트러블슈팅

### 문제 1: Redis 연결 실패
```bash
# 증상
ERROR: Failed to connect to Redis

# 해결
docker-compose -f docker-compose.prod.yml up -d redis
docker restart uvis-backend
```

### 문제 2: ML 추천이 표시되지 않음
```bash
# 증상
프론트엔드에서 ML 추천 패널이 보이지 않음

# 확인
# 1. 백엔드 로그 확인
docker logs uvis-backend --tail 50

# 2. API 엔드포인트 테스트
curl http://139.150.11.99:8000/api/ml-dispatch/optimize?mode=recommend \
  -X POST -H "Content-Type: application/json" \
  -d '{"order_ids": [1, 2, 3]}'

# 3. 프론트엔드 재빌드
docker-compose -f docker-compose.prod.yml up -d --build frontend
```

### 문제 3: 자동 롤백이 작동하지 않음
```bash
# 증상
성능 저하에도 불구하고 롤백되지 않음

# 확인
# 1. Auto Rollback 상태 확인
curl http://139.150.11.99:8000/api/ml-dispatch/rollback/status

# 2. Redis 상태 확인
docker exec -it uvis-redis redis-cli
> GET ml_dispatch:rollback:cooldown

# 3. 백엔드 로그 확인
docker logs uvis-backend | grep -i rollback
```

### 문제 4: 롤아웃 비율이 반영되지 않음
```bash
# 증상
롤아웃 비율을 변경했지만 사용자에게 적용되지 않음

# 해결
# Redis 캐시 초기화
docker exec -it uvis-redis redis-cli FLUSHDB

# 백엔드 재시작
docker restart uvis-backend
```

---

## 📝 체크리스트

### 배포 전
- [ ] Redis 서비스 실행 확인
- [ ] 환경 변수 설정 (REDIS_HOST, REDIS_PORT)
- [ ] 백업 생성 (코드 & DB)
- [ ] 롤백 계획 수립

### 배포 중
- [ ] Git pull 및 코드 배포
- [ ] 백엔드 재빌드 및 재시작
- [ ] 프론트엔드 재빌드 및 재시작
- [ ] API 엔드포인트 동작 확인

### 배포 후
- [ ] 파일럿 롤아웃 (10%)
- [ ] 1시간 모니터링
- [ ] 성공 기준 충족 확인
- [ ] 점진적 확대 (30% → 50% → 100%)
- [ ] 자동 롤백 시스템 동작 확인

### 전면 배포 후
- [ ] ML 배차를 기본으로 설정
- [ ] 기존 시스템 백업 유지
- [ ] 지속적 모니터링 (1개월)
- [ ] 사용자 교육 및 피드백 수집

---

## 🎉 완료!

Phase 3 배포를 완료하면 다음과 같은 시스템이 구축됩니다:

✅ **안전한 A/B 테스트 프레임워크**
✅ **점진적 롤아웃 (10% → 100%)**
✅ **실시간 성과 모니터링**
✅ **자동 롤백 메커니즘**
✅ **프로덕션 준비 완료**

---

## 📚 관련 문서

- [Phase 1 Implementation Guide](PHASE1_IMPLEMENTATION_GUIDE.md)
- [Phase 2 Deployment Guide](PHASE2_DEPLOYMENT_GUIDE.md)
- [ML Dispatch Architecture](ML_DISPATCH_ARCHITECTURE.md)
- [ML Deployment Guide](ML_DEPLOYMENT_GUIDE.md)
- [ML Quick Start](ML_QUICK_START.md)

---

## 📞 지원

문제가 발생하면:
1. 로그 확인: `docker logs uvis-backend --tail 100`
2. API 문서: `http://139.150.11.99:8000/docs`
3. GitHub Issues: 이슈 등록
